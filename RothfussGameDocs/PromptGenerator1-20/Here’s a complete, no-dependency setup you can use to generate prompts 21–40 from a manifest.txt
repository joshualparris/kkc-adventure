The cleanest way to automate prompts 21–40 is to treat them as data + template, not as one-off writing. Your roadmap already gives you the sequence for 21–40: consequence triggers, delayed reputation spread, Archives access, basic sympathy action, canon events, tuition hearing, first Eolian audition, world state manager, Ambrose escalation, calendar, CLI/save flow, and then testing/tone review.
Best approach
Build three small things:
1. a prompt manifest for prompts 21–40
2. a master prompt template
3. a generator script that merges them
That way, you are not asking Claude or Gemini to invent the structure each time. You are only asking it to fill in controlled slots.
What the manifest should hold
Use one JSON or YAML entry per prompt.
Example fields:
* number
* title
* goal
* scope
* out_of_scope
* depends_on
* new_files
* modified_files
* tests
* success_criteria
* notes_from_original_plan
For 21–40, the manifest can map directly to your roadmap items. For example:
* 21: reputation consequence triggers
* 22: pending reputation queue / “word travels”
* 23: Archives access system
* 24: basic sympathy action
* 25–30: canon event system, tuition hearing, Eolian audition, world state, Ambrose escalation, calendar
* 31–35: CLI polish and save/load
* 36–40: integration tests, lore/tone checks, load test, full-pass playthrough.
Master template
Use one fixed template for every generated prompt. Something like:
KKC TEXT ADVENTURE — PROMPT {{number}} OF 180
==========================================
{{title}}
==========================================


This prompt builds directly on Prompts 1 through {{previous_prompt}}.
Do not restructure, rename, or refactor anything from those prompts.
Only add to what exists.


This prompt follows the original Phase 1 plan role for Prompt {{number}}:
{{goal}}


HARD BANS — UNCHANGED
{{hard_bans}}


ARCHITECTURE RULE — STILL ABSOLUTE
{{architecture_rule}}


GOAL OF THIS PROMPT
{{goal_detail}}


SCOPE OF THIS PROMPT
{{scope}}


This prompt does NOT cover:
{{out_of_scope}}


NEW FILES IN THIS PROMPT
{{new_files}}


Modify these existing files:
{{modified_files}}


IMPLEMENTATION DETAILS
{{implementation_sections}}


TESTS
{{tests}}


ARCHITECTURE REMINDERS
{{architecture_reminders}}


SUCCESS CRITERIA
{{success_criteria}}


END OF PROMPT {{number}}


The point is that Claude is no longer deciding the skeleton. It only fills implementation_sections from your manifest and the original plan.
Generator script
A simple Python script is enough.
import json
from pathlib import Path
from string import Template


manifest = json.loads(Path("prompt_manifest_21_40.json").read_text())
template = Template(Path("prompt_template.txt").read_text())


OUT = Path("generated_prompts")
OUT.mkdir(exist_ok=True)


for item in manifest:
    text = template.substitute(
        number=item["number"],
        previous_prompt=item["number"] - 1,
        title=item["title"],
        goal=item["goal"],
        hard_bans=item["hard_bans"],
        architecture_rule=item["architecture_rule"],
        goal_detail=item["goal_detail"],
        scope="\n".join(f"  {i+1}. {x}" for i, x in enumerate(item["scope"])),
        out_of_scope="\n".join(f"  - {x}" for x in item["out_of_scope"]),
        new_files="\n".join(f"  {x}" for x in item["new_files"]),
        modified_files="\n".join(f"  {x}" for x in item["modified_files"]),
        implementation_sections=item["implementation_sections"],
        tests=item["tests"],
        architecture_reminders=item["architecture_reminders"],
        success_criteria=item["success_criteria"],
    )
    Path(OUT / f"prompt_{item['number']:02}.md").write_text(text)


Where AI should help
Use AI for only two parts:
1. Drafting implementation_sections
Feed it:
* the original roadmap item
* your prompt rules from 1–20
* the current file list / architecture rules
* the last 2–3 prompts for continuity
Then ask it to output structured JSON, not prose.
Example generation prompt:
Given Prompt 21 in my roadmap, produce JSON with:
- title
- goal
- scope
- out_of_scope
- new_files
- modified_files
- implementation_sections
- tests
- success_criteria


Rules:
- preserve engine-as-truth architecture
- no refactors
- no new systems outside Prompt 21
- use my existing prompt style
- return JSON only


2. Reviewing the generated prompt
After the script renders the markdown, ask AI:
* does this duplicate prior systems?
* does this violate scope?
* does it add hidden refactors?
* does it drift from the original roadmap item?
That is much safer than asking AI to write every prompt from scratch.
Keep a “continuity file”
This matters a lot.
Make one file like project_state.md with:
* prompts already completed
* files currently expected to exist
* active systems already introduced
* current architecture rules
* things intentionally deferred
Then prepend that into every AI generation request.
Otherwise the model will slowly forget what has already been standardised.
Practical file setup
I’d use:
prompt-gen/
  prompt_template.txt
  prompt_manifest_21_40.json
  project_state.md
  hard_bans.txt
  architecture_rule.txt
  generated_prompts/


Strong recommendation for 21–40
Don’t generate 20 full prompts in one shot.
Generate in blocks of 5:
* 21–25
* 26–30
* 31–35
* 36–40
That matches the way your roadmap clusters work anyway:
* 21–24 are reputation / Archives / sympathy foundations
* 25–30 are canon events + world/calendar
* 31–35 are interface/save flow
* 36–40 are testing/review.
This gives you a chance to correct drift before it compounds.
Best guardrails
Every generated prompt should automatically include:
* “Do not restructure, rename, or refactor”
* “Only add to what exists”
* “Skip missing files rather than inventing around them”
* “No new gameplay systems beyond this prompt’s scope”
* “All new tests must pass alongside earlier prompts’ tests”
That keeps generation disciplined.
My honest recommendation
The safest automation loop is:
1. You define the manifest entries
2. AI drafts structured JSON for each prompt
3. A script renders the final prompt text
4. You review and lightly edit
5. Then feed the prompt to Codex/Claude
That gives you automation without surrendering design control.
If you want, I can write you:
* a ready-to-use manifest for prompts 21–25, or
* the full generator script + template.




Here’s a complete, no-dependency setup you can use to generate prompts 21–40 from a manifest.
Your original roadmap already groups those prompts into clear clusters — consequence triggers, delayed reputation spread, Archives/sympathy, canon events, CLI/save flow, and testing/review — so this generator is built to preserve that structure instead of improvising each prompt from scratch.
1. generate_prompts.py
#!/usr/bin/env python3
"""
Generate KKC prompt files from a JSON manifest and a markdown template.


No third-party dependencies required.
Uses:
- prompt_template.md
- prompt_manifest_21_40.json
- optional project_state.md
- optional hard_bans.md
- optional architecture_rule.md
- optional architecture_reminders.md


Outputs one .md file per prompt into ./generated_prompts by default.
"""


from __future__ import annotations


import argparse
import json
import re
import sys
from pathlib import Path
from typing import Any, Dict, Iterable, List, Optional




ROOT = Path(".")
DEFAULT_TEMPLATE = ROOT / "prompt_template.md"
DEFAULT_MANIFEST = ROOT / "prompt_manifest_21_40.json"
DEFAULT_OUTPUT_DIR = ROOT / "generated_prompts"


OPTIONAL_CONTEXT_FILES = {
    "project_state": ROOT / "project_state.md",
    "hard_bans": ROOT / "hard_bans.md",
    "architecture_rule": ROOT / "architecture_rule.md",
    "architecture_reminders": ROOT / "architecture_reminders.md",
}




REQUIRED_FIELDS = [
    "number",
    "title",
    "phase_role",
    "goal",
    "goal_detail",
    "scope",
    "out_of_scope",
    "new_files",
    "modified_files",
    "implementation_sections",
    "tests",
    "success_criteria",
]




def read_text(path: Path, default: str = "") -> str:
    return path.read_text(encoding="utf-8") if path.exists() else default




def normalise_newlines(text: str) -> str:
    return text.replace("\r\n", "\n").replace("\r", "\n").strip()




def format_bullets(items: Iterable[str], indent: str = "  - ") -> str:
    items = [str(x).strip() for x in items if str(x).strip()]
    return "\n".join(f"{indent}{item}" for item in items) if items else f"{indent}(none)"




def format_numbered(items: Iterable[str], indent: str = "  ") -> str:
    items = [str(x).strip() for x in items if str(x).strip()]
    return "\n".join(f"{indent}{i + 1}. {item}" for i, item in enumerate(items)) if items else f"{indent}1. (none)"




def slugify(text: str) -> str:
    text = text.lower().strip()
    text = re.sub(r"[^a-z0-9]+", "_", text)
    return re.sub(r"_+", "_", text).strip("_")




def validate_prompt_entry(entry: Dict[str, Any]) -> List[str]:
    errors: List[str] = []


    for field in REQUIRED_FIELDS:
        if field not in entry:
            errors.append(f"Missing required field: {field}")


    if "number" in entry and not isinstance(entry["number"], int):
        errors.append("Field 'number' must be an integer")


    list_fields = [
        "scope",
        "out_of_scope",
        "new_files",
        "modified_files",
        "success_criteria",
    ]
    for field in list_fields:
        if field in entry and not isinstance(entry[field], list):
            errors.append(f"Field '{field}' must be a list")


    string_fields = [
        "title",
        "phase_role",
        "goal",
        "goal_detail",
        "implementation_sections",
        "tests",
    ]
    for field in string_fields:
        if field in entry and not isinstance(entry[field], str):
            errors.append(f"Field '{field}' must be a string")


    return errors




def load_manifest(path: Path) -> List[Dict[str, Any]]:
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except FileNotFoundError:
        raise SystemExit(f"Manifest not found: {path}")
    except json.JSONDecodeError as exc:
        raise SystemExit(f"Manifest is not valid JSON: {exc}")


    if not isinstance(data, list):
        raise SystemExit("Manifest root must be a JSON array")


    errors: List[str] = []
    seen_numbers = set()


    for idx, entry in enumerate(data):
        if not isinstance(entry, dict):
            errors.append(f"Entry {idx} is not an object")
            continue


        entry_errors = validate_prompt_entry(entry)
        if entry_errors:
            errors.extend([f"Entry {idx}: {msg}" for msg in entry_errors])


        number = entry.get("number")
        if isinstance(number, int):
            if number in seen_numbers:
                errors.append(f"Duplicate prompt number: {number}")
            seen_numbers.add(number)


    if errors:
        joined = "\n".join(f"- {e}" for e in errors)
        raise SystemExit(f"Manifest validation failed:\n{joined}")


    return data




def render_template(template: str, values: Dict[str, str]) -> str:
    missing = []


    def replacer(match: re.Match[str]) -> str:
        key = match.group(1)
        if key not in values:
            missing.append(key)
            return match.group(0)
        return values[key]


    rendered = re.sub(r"\{\{([a-zA-Z0-9_]+)\}\}", replacer, template)


    if missing:
        missing_list = ", ".join(sorted(set(missing)))
        raise SystemExit(f"Template contains unresolved placeholders: {missing_list}")


    return rendered.strip() + "\n"




def build_values(
    entry: Dict[str, Any],
    global_context: Dict[str, str],
) -> Dict[str, str]:
    number = entry["number"]
    previous_prompt = str(number - 1)


    title = str(entry["title"]).strip()
    filename_slug = slugify(title)


    values: Dict[str, str] = {
        "number": str(number),
        "previous_prompt": previous_prompt,
        "title": title,
        "phase_role": str(entry["phase_role"]).strip(),
        "goal": str(entry["goal"]).strip(),
        "goal_detail": normalise_newlines(str(entry["goal_detail"])),
        "scope": format_numbered(entry["scope"]),
        "out_of_scope": format_bullets(entry["out_of_scope"]),
        "new_files": format_bullets(entry["new_files"]),
        "modified_files": format_bullets(entry["modified_files"]),
        "implementation_sections": normalise_newlines(str(entry["implementation_sections"])),
        "tests": normalise_newlines(str(entry["tests"])),
        "success_criteria": format_bullets(entry["success_criteria"]),
        "hard_bans": global_context["hard_bans"],
        "architecture_rule": global_context["architecture_rule"],
        "architecture_reminders": global_context["architecture_reminders"],
        "project_state": global_context["project_state"],
        "filename_slug": filename_slug,
    }


    return values




def select_entries(
    manifest: List[Dict[str, Any]],
    start: Optional[int],
    end: Optional[int],
    only: Optional[List[int]],
) -> List[Dict[str, Any]]:
    entries = manifest


    if only:
        wanted = set(only)
        entries = [e for e in entries if e["number"] in wanted]


    if start is not None:
        entries = [e for e in entries if e["number"] >= start]


    if end is not None:
        entries = [e for e in entries if e["number"] <= end]


    entries = sorted(entries, key=lambda e: e["number"])


    if not entries:
        raise SystemExit("No prompt entries selected")


    return entries




def parse_only_list(value: Optional[str]) -> Optional[List[int]]:
    if not value:
        return None
    nums = []
    for chunk in value.split(","):
        chunk = chunk.strip()
        if not chunk:
            continue
        try:
            nums.append(int(chunk))
        except ValueError:
            raise SystemExit(f"Invalid prompt number in --only: {chunk}")
    return nums or None




def build_global_context() -> Dict[str, str]:
    return {
        "project_state": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["project_state"], "")),
        "hard_bans": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["hard_bans"], "HARD BANS — UNCHANGED")),
        "architecture_rule": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["architecture_rule"], "ARCHITECTURE RULE — STILL ABSOLUTE")),
        "architecture_reminders": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["architecture_reminders"], "These rules from Prompt 1 still apply.")),
    }




def main() -> None:
    parser = argparse.ArgumentParser(description="Generate KKC prompt markdown files from a manifest.")
    parser.add_argument("--template", type=Path, default=DEFAULT_TEMPLATE, help="Path to prompt template markdown")
    parser.add_argument("--manifest", type=Path, default=DEFAULT_MANIFEST, help="Path to JSON manifest")
    parser.add_argument("--out", type=Path, default=DEFAULT_OUTPUT_DIR, help="Output directory")
    parser.add_argument("--start", type=int, help="First prompt number to generate")
    parser.add_argument("--end", type=int, help="Last prompt number to generate")
    parser.add_argument("--only", type=str, help="Comma-separated prompt numbers to generate, e.g. 21,22,25")
    parser.add_argument("--dry-run", action="store_true", help="Print generated prompts instead of writing files")
    args = parser.parse_args()


    template = read_text(args.template)
    if not template:
        raise SystemExit(f"Template not found or empty: {args.template}")


    manifest = load_manifest(args.manifest)
    only = parse_only_list(args.only)
    entries = select_entries(manifest, args.start, args.end, only)
    global_context = build_global_context()


    if not args.dry_run:
        args.out.mkdir(parents=True, exist_ok=True)


    for entry in entries:
        values = build_values(entry, global_context)
        rendered = render_template(template, values)


        filename = f"prompt_{entry['number']:02}_{values['filename_slug']}.md"
        output_path = args.out / filename


        if args.dry_run:
            divider = "=" * 80
            print(divider)
            print(filename)
            print(divider)
            print(rendered)
        else:
            output_path.write_text(rendered, encoding="utf-8")
            print(f"Wrote {output_path}")




if __name__ == "__main__":
    main()


2. prompt_template.md
KKC TEXT ADVENTURE — PROMPT {{number}} OF 180
==========================================
{{title}}
==========================================


This prompt builds directly on Prompts 1 through {{previous_prompt}}.
Do not restructure, rename, or refactor anything from those prompts.
Only add to what exists.


This prompt follows the original Phase 1 plan role for Prompt {{number}}:
{{phase_role}}


Current project state:
{{project_state}}


═══════════════════════════════════════
HARD BANS — UNCHANGED
═══════════════════════════════════════


{{hard_bans}}


═══════════════════════════════════════
ARCHITECTURE RULE — STILL ABSOLUTE
═══════════════════════════════════════


{{architecture_rule}}


═══════════════════════════════════════
GOAL OF THIS PROMPT
═══════════════════════════════════════


{{goal}}


{{goal_detail}}


═══════════════════════════════════════
SCOPE OF THIS PROMPT
═══════════════════════════════════════


This prompt covers:
{{scope}}


This prompt does NOT cover:
{{out_of_scope}}


═══════════════════════════════════════
NEW FILES IN THIS PROMPT
═══════════════════════════════════════


Add exactly these files:
{{new_files}}


Modify these existing files:
{{modified_files}}


Do not add any other files.


═══════════════════════════════════════
IMPLEMENTATION DETAILS
═══════════════════════════════════════


{{implementation_sections}}


═══════════════════════════════════════
TESTS
═══════════════════════════════════════


{{tests}}


═══════════════════════════════════════
ARCHITECTURE REMINDERS
═══════════════════════════════════════


{{architecture_reminders}}


═══════════════════════════════════════
SUCCESS CRITERIA
═══════════════════════════════════════


This prompt is complete when:
{{success_criteria}}


==========================================
END OF PROMPT {{number}}
==========================================


3. prompt_manifest_21_40.json
This is a starter manifest for prompts 21–40.
It is intentionally concise in some places so you can refine the implementation sections gradually.
[
  {
    "number": 21,
    "title": "Reputation Consequence Triggers",
    "phase_role": "write reputation consequence triggers so academic and social standing start affecting reactions and access in modest, deterministic ways.",
    "goal": "Introduce consequence triggers on top of the centralised reputation system without adding a huge new world-simulation layer.",
    "goal_detail": "Focus on small, testable consequences. Examples: low academic standing affects how certain Masters or academic-facing systems respond; very poor trust with Ambrose-related social channels can make specific interactions more hostile. Keep this modest and deterministic.",
    "scope": [
      "Add engine/reputationConsequences.ts",
      "Define trigger helpers for academic standing and selected NPC trust bands",
      "Wire modest consequence checks into existing relevant flows",
      "Add tests for trigger thresholds and non-trigger cases"
    ],
    "out_of_scope": [
      "Delayed reputation spread",
      "Dynamic NPC schedules",
      "Large-scale hostility systems",
      "New commands"
    ],
    "new_files": [
      "src/engine/reputationConsequences.ts",
      "tests/reputationConsequences.test.ts"
    ],
    "modified_files": [
      "src/engine/actions.ts",
      "src/engine/npcEngine.ts",
      "src/narration/renderStatus.ts"
    ],
    "implementation_sections": "Implement small, deterministic consequence helpers keyed off the shared reputation engine. Keep effects local and readable. Do not build a faction framework. Use threshold bands and explicit checks. If a consequence message is needed, keep it brief and in-world.",
    "tests": "Add tests for low academic standing trigger, no-trigger when standing is decent, and an Ambrose-trust-based social hostility check that remains local rather than global.",
    "success_criteria": [
      "reputation consequences are centralised and deterministic",
      "existing systems can query consequence state without duplicating threshold logic",
      "no delayed spread system is introduced yet",
      "all new tests pass"
    ]
  },
  {
    "number": 22,
    "title": "Word Travels: Pending Reputation Changes Queue",
    "phase_role": "add a delayed reputation spread queue so some changes reach the wider world after 1–3 in-game days instead of instantly.",
    "goal": "Make the world feel more socially believable by separating immediate internal changes from delayed wider awareness.",
    "goal_detail": "Keep the delayed queue narrow and mechanical. Immediate direct trust changes can remain immediate; broader public-facing reputation shifts can queue and apply later.",
    "scope": [
      "Add engine/reputationQueue.ts",
      "Use pending_reputation_changes table or existing persistence structure",
      "Apply queued changes when day advances",
      "Add tests for delay and one-time application"
    ],
    "out_of_scope": [
      "Large social simulation",
      "Dynamic rumour spread",
      "NPC memory systems",
      "New commands"
    ],
    "new_files": [
      "src/engine/reputationQueue.ts",
      "tests/reputationQueue.test.ts"
    ],
    "modified_files": [
      "src/engine/time.ts",
      "src/engine/socialEngine.ts",
      "src/engine/musicEngine.ts"
    ],
    "implementation_sections": "Implement a small pending queue abstraction. Queue public-facing changes with apply_on_day, then apply and clear them when the current day reaches that threshold. Keep ordering deterministic. Avoid background complexity.",
    "tests": "Add tests for queuing a change, not applying it early, applying it on the correct day, and not applying it twice.",
    "success_criteria": [
      "pending public reputation changes can be delayed",
      "queued changes apply on the correct day",
      "queued changes apply only once",
      "existing immediate systems still work"
    ]
  },
  {
    "number": 23,
    "title": "Archives Access System",
    "phase_role": "formalise Archives access as requiring Re'lar rank and Lorren's approval, with proper in-world denial handling.",
    "goal": "Turn the existing Archives restrictions into one clear system rather than scattered checks.",
    "goal_detail": "Use the canon-safe MVP rule already established: the Stacks require Re'lar and Lorren's approval. Keep denial grounded and institutional.",
    "scope": [
      "Add engine/archivesAccess.ts",
      "Track Lorren approval in world/player state if needed",
      "Centralise access checks and denial text",
      "Add tests for rank and approval combinations"
    ],
    "out_of_scope": [
      "Full Archives browsing",
      "Lorren as a full active NPC system",
      "Research mechanics",
      "New map areas beyond current Archives spaces"
    ],
    "new_files": [
      "src/engine/archivesAccess.ts",
      "tests/archivesAccess.test.ts"
    ],
    "modified_files": [
      "src/types/index.ts",
      "src/engine/movement.ts",
      "src/narration/renderLocation.ts"
    ],
    "implementation_sections": "Create one Archives access helper that determines whether the player may enter the Stacks and returns a clean in-world failure line when blocked. Reuse existing rank and canon registry truth.",
    "tests": "Add tests for blocked E'lir, blocked Re'lar without approval if approval tracking exists, and successful access when all conditions are met.",
    "success_criteria": [
      "Archives access logic is centralised",
      "the Stacks require Re'lar and approval",
      "denial messages are grounded and consistent",
      "movement/rendering respect the shared access logic"
    ]
  },
  {
    "number": 24,
    "title": "Basic Sympathy Action Standardisation",
    "phase_role": "turn the early sympathy system into one clear MVP action path with deterministic pass/fail and consistent consequences.",
    "goal": "Consolidate sympathy action handling so later prompts can build on a stable core.",
    "goal_detail": "Use the systems already introduced: links, warmth/body heat, materials, and simple backlash/slip outcomes. This is not full naming or advanced sympathy depth yet.",
    "scope": [
      "Add engine/sympathyAction.ts or equivalent central module",
      "Route existing sympathy use through shared helpers",
      "Standardise consequences and result text hooks",
      "Add tests for successful, failed, and unsafe actions"
    ],
    "out_of_scope": [
      "Advanced sympathy chaining",
      "Naming",
      "Alchemy",
      "Crafting"
    ],
    "new_files": [
      "src/engine/sympathyAction.ts",
      "tests/sympathyAction.test.ts"
    ],
    "modified_files": [
      "src/engine/actions.ts",
      "src/engine/sympathyEngine.ts",
      "src/narration/narrationContext.ts"
    ],
    "implementation_sections": "Keep the logic deterministic and compact. The engine decides if the action works, what it costs, and what consequence bucket it lands in. The narration layer only expresses it.",
    "tests": "Add tests for a valid action, a blocked action due to missing material or unsafe condition, and a failed action with an appropriate consequence bucket.",
    "success_criteria": [
      "sympathy action handling is centralised",
      "the engine owns all success/failure truth",
      "later prompts can build on this shared path",
      "all new tests pass"
    ]
  },
  {
    "number": 25,
    "title": "Canon Event System",
    "phase_role": "add a small event system where fixed or semi-fixed events can trigger from world conditions without railroading the player.",
    "goal": "Create the structural layer needed for tuition hearing, first Eolian audition framing, and later University events.",
    "goal_detail": "Keep the event system modest: trigger conditions, fixed vs flexible framing, and world state changes. Do not explode into branching narrative infrastructure.",
    "scope": [
      "Add engine/eventEngine.ts",
      "Define event shape and trigger condition checks",
      "Add world-state updates on event resolution",
      "Add tests for trigger evaluation"
    ],
    "out_of_scope": [
      "Large branching story graphs",
      "Cinematic scripting",
      "Multi-era story management",
      "Frontend event UI"
    ],
    "new_files": [
      "src/engine/eventEngine.ts",
      "tests/eventEngine.test.ts"
    ],
    "modified_files": [
      "src/types/index.ts",
      "src/engine/actions.ts",
      "src/engine/state.ts"
    ],
    "implementation_sections": "Implement a compact event model with id, trigger_conditions, is_fixed, and world_state_changes. Check triggers after actions. Keep it deterministic and engine-owned.",
    "tests": "Add tests for unmet triggers, met triggers, single-fire events, and world-state updates.",
    "success_criteria": [
      "events can trigger from world conditions",
      "world_state changes are applied deterministically",
      "the system is ready for concrete Phase 1 events",
      "all new tests pass"
    ]
  },
  {
    "number": 26,
    "title": "Tuition Hearing Event",
    "phase_role": "script the tuition hearing as a canon-grounded fixed event whose framing can vary but whose institutional reality remains intact.",
    "goal": "Use the event system to make tuition feel like a real University pressure point.",
    "goal_detail": "Keep the hearing modest and text-centric. The player can affect approach and flavour, but the event remains institutionally bounded.",
    "scope": [
      "Add authored tuition hearing event definition",
      "Tie it to tuition and term conditions",
      "Add outcome hooks for academic standing/social pressure",
      "Add tests for trigger and outcome application"
    ],
    "out_of_scope": [
      "Full Masters dialogue roster",
      "Large branching tribunal scenes",
      "Rank advancement decisions"
    ],
    "new_files": [
      "src/content/events/tuitionHearing.ts",
      "tests/tuitionHearingEvent.test.ts"
    ],
    "modified_files": [
      "src/engine/eventEngine.ts",
      "src/engine/tuitionEngine.ts"
    ],
    "implementation_sections": "Use the event system from Prompt 25. Keep the event fixed in nature but allow small flavour variations based on state. Do not over-script every Master yet.",
    "tests": "Add tests for correct trigger conditions and correct world/reputation changes after the event.",
    "success_criteria": [
      "the tuition hearing can trigger correctly",
      "the event feels institutional and grounded",
      "the system remains deterministic",
      "all new tests pass"
    ]
  },
  {
    "number": 27,
    "title": "First Eolian Audition Event Framing",
    "phase_role": "script the first Eolian audition as a proper event wrapper around the existing music logic.",
    "goal": "Make the first serious attempt for pipes feel like an event, not just a mechanical function call.",
    "goal_detail": "Use the existing audition logic and event system together. The player’s prep and condition matter, but the structure remains controlled.",
    "scope": [
      "Add an Eolian audition event definition",
      "Use existing music/audition truth",
      "Add event framing and world-state updates",
      "Add tests"
    ],
    "out_of_scope": [
      "New music mechanics",
      "Denna scene scripting",
      "Concert-style multi-stage event trees"
    ],
    "new_files": [
      "src/content/events/eolianFirstAudition.ts",
      "tests/eolianFirstAuditionEvent.test.ts"
    ],
    "modified_files": [
      "src/engine/eventEngine.ts",
      "src/engine/musicEngine.ts"
    ],
    "implementation_sections": "Wrap the existing audition logic rather than replacing it. The event layer should frame the moment and update world state, but not own music truth.",
    "tests": "Add tests for trigger conditions, successful world-state update, and correct use of underlying audition truth.",
    "success_criteria": [
      "the first audition can be framed as an event",
      "the event uses existing music truth rather than duplicating it",
      "world state can record that it happened",
      "all new tests pass"
    ]
  },
  {
    "number": 28,
    "title": "World State Manager",
    "phase_role": "formalise world state tracking so flags, term context, and event markers live in one coherent place.",
    "goal": "Make world_state a first-class shared structure rather than a loose collection of flags.",
    "goal_detail": "Focus on coherent access/update helpers, not on adding lots of new flags.",
    "scope": [
      "Add engine/worldStateEngine.ts",
      "Centralise get/set/update helpers",
      "Standardise common Phase 1 flags",
      "Add tests for state mutation and retrieval"
    ],
    "out_of_scope": [
      "Save-slot systems",
      "Branch visualisation",
      "Narrative debugging UI"
    ],
    "new_files": [
      "src/engine/worldStateEngine.ts",
      "tests/worldStateEngine.test.ts"
    ],
    "modified_files": [
      "src/engine/state.ts",
      "src/engine/eventEngine.ts"
    ],
    "implementation_sections": "Keep helpers small and deterministic. This is a coherence pass for flags like has_met_X, term_number, days_until_tuition, event markers, and access markers.",
    "tests": "Add tests for world-state get/set/update behaviour and compatibility with persisted player/world state.",
    "success_criteria": [
      "world_state has a shared management layer",
      "flags become easier to reason about",
      "event and progression systems can rely on one shared place",
      "all new tests pass"
    ]
  },
  {
    "number": 29,
    "title": "Ambrose Conflict Escalation",
    "phase_role": "turn Ambrose pressure into a staged escalation system rather than isolated incidents.",
    "goal": "Make Ambrose feel like a persistent social threat without overbuilding a villain system.",
    "goal_detail": "Use small, deterministic stages. This is still University-level tension, not later-book-scale plotting.",
    "scope": [
      "Add ambroseEscalation.ts",
      "Track escalation stage",
      "Tie stage changes to social interactions and trust bands",
      "Add tests for escalation progression"
    ],
    "out_of_scope": [
      "Sabotage scenes",
      "Theft plots",
      "Plum bob",
      "Major disciplinary hearings"
    ],
    "new_files": [
      "src/engine/ambroseEscalation.ts",
      "tests/ambroseEscalation.test.ts"
    ],
    "modified_files": [
      "src/engine/socialEngine.ts",
      "src/engine/worldStateEngine.ts"
    ],
    "implementation_sections": "Use a small stage model, e.g. quiet hostility -> public cuttingness -> formal trouble pressure. Keep consequences local and deterministic.",
    "tests": "Add tests for stage progression, no-jump behaviour, and stable persistence.",
    "success_criteria": [
      "Ambrose conflict now escalates coherently",
      "the escalation remains modest and Phase-1 appropriate",
      "world state can remember the stage",
      "all new tests pass"
    ]
  },
  {
    "number": 30,
    "title": "Calendar and Term Structure",
    "phase_role": "add a simple calendar so days, weeks, and term framing become explicit and deadlines sit on a real timeline.",
    "goal": "Make time feel like a University term rather than just a day counter.",
    "goal_detail": "Keep the calendar simple and mechanical. This is for deadline realism, not for seasonal simulation.",
    "scope": [
      "Add engine/calendarEngine.ts",
      "Map day numbers into weeks/term context",
      "Expose helpers for deadline/date text",
      "Add tests"
    ],
    "out_of_scope": [
      "Season/weather simulation",
      "Holiday scheduling",
      "Multi-term generation"
    ],
    "new_files": [
      "src/engine/calendarEngine.ts",
      "tests/calendarEngine.test.ts"
    ],
    "modified_files": [
      "src/narration/renderStatus.ts",
      "src/engine/tuitionEngine.ts"
    ],
    "implementation_sections": "Implement a light calendar abstraction around the existing day_number/term_number rather than replacing them. Keep it deterministic and useful for status/deadlines.",
    "tests": "Add tests for week/day mapping and stable deadline text generation.",
    "success_criteria": [
      "the game can express time as part of a term structure",
      "deadline systems can refer to the calendar cleanly",
      "existing day tracking remains compatible",
      "all new tests pass"
    ]
  },
  {
    "number": 31,
    "title": "CLI Status Header",
    "phase_role": "improve the REPL interface so each turn surfaces location, time, and compact condition info more cleanly.",
    "goal": "Make the terminal experience more readable without changing underlying systems.",
    "goal_detail": "This is a usability/presentation pass, not a frontend rewrite.",
    "scope": [
      "Add a reusable status header renderer",
      "Show location/time/condition summary above narration blocks",
      "Keep the CLI compact",
      "Add tests where practical"
    ],
    "out_of_scope": [
      "Web UI",
      "Rich text formatting",
      "Maps or side panels"
    ],
    "new_files": [
      "src/narration/renderHeader.ts",
      "tests/renderHeader.test.ts"
    ],
    "modified_files": [
      "src/repl.ts",
      "src/narration/renderStatus.ts"
    ],
    "implementation_sections": "Keep the interface plain and readable. Use one compact header block before scene text. Avoid vertical bloat.",
    "tests": "Add snapshot-style or string-shape tests for compact header output.",
    "success_criteria": [
      "the CLI shows a cleaner status header",
      "readability improves without changing systems",
      "all new tests pass"
    ]
  },
  {
    "number": 32,
    "title": "Command Parsing Expansion",
    "phase_role": "expand command parsing to support the wider MVP verb set cleanly and deterministically.",
    "goal": "Consolidate the growing command surface so it stays readable and testable.",
    "goal_detail": "The verbs are already emerging across prompts. This prompt makes parsing consistent rather than adding whole new mechanics.",
    "scope": [
      "Standardise command parsing patterns",
      "Support existing verbs cleanly",
      "Improve parser tests",
      "Keep logic deterministic"
    ],
    "out_of_scope": [
      "Natural-language free parsing",
      "LLM command understanding",
      "New core mechanics"
    ],
    "new_files": [
      "src/engine/commandParser.ts",
      "tests/commandParser.test.ts"
    ],
    "modified_files": [
      "src/engine/actions.ts",
      "src/engine/npcEngine.ts"
    ],
    "implementation_sections": "Extract the repetitive command matching into a central parser if and only if that reduces duplication without a large refactor. Keep command families explicit.",
    "tests": "Add tests for go/talk/ask/examine/use/listen/status/help and ambiguous edge cases.",
    "success_criteria": [
      "command parsing is more coherent",
      "existing commands still work",
      "no freeform parser overreach is introduced",
      "all new tests pass"
    ]
  },
  {
    "number": 33,
    "title": "In-World Help Command",
    "phase_role": "improve the help command so it remains useful without breaking immersion.",
    "goal": "Make help clearer, context-aware where practical, and still in-world.",
    "goal_detail": "Use the command set that actually exists by this point. Keep the tone natural and concise.",
    "scope": [
      "Centralise help text rendering",
      "Reflect currently available command families",
      "Keep tone in-world",
      "Add tests"
    ],
    "out_of_scope": [
      "Tutorial mode",
      "Interactive onboarding flow",
      "Context-sensitive walkthroughs"
    ],
    "new_files": [
      "src/narration/renderHelp.ts",
      "tests/renderHelp.test.ts"
    ],
    "modified_files": [
      "src/narration/localNarrator.ts",
      "src/engine/actions.ts"
    ],
    "implementation_sections": "Turn help into a maintained renderer rather than scattered strings. Keep it short and useful.",
    "tests": "Add tests for output containing the current major verb families without using gamey language.",
    "success_criteria": [
      "help remains in-world and useful",
      "the command set is represented accurately",
      "all new tests pass"
    ]
  },
  {
    "number": 34,
    "title": "Save and Load System",
    "phase_role": "implement save/load so player_state and world_state can persist beyond the SQLite session if needed.",
    "goal": "Add explicit save/load files on top of the existing persistence model.",
    "goal_detail": "Keep the format simple and transparent. This is about reliability, not multiple save-slot UX.",
    "scope": [
      "Add saveLoadEngine.ts",
      "Serialise player_state and world_state to JSON",
      "Support save and load helpers",
      "Add tests for round-trip correctness"
    ],
    "out_of_scope": [
      "Multiple save slots",
      "Cloud sync",
      "Encryption",
      "UI save menus"
    ],
    "new_files": [
      "src/engine/saveLoadEngine.ts",
      "tests/saveLoadEngine.test.ts"
    ],
    "modified_files": [
      "src/repl.ts",
      "src/engine/state.ts"
    ],
    "implementation_sections": "Use plain JSON files with clear structure. Keep save/load explicit and testable. Do not replace the SQLite backing store; layer on top carefully.",
    "tests": "Add tests for save file creation, load round-trip, and graceful handling of missing files.",
    "success_criteria": [
      "state can be serialised and loaded cleanly",
      "round-trip preserves core fields",
      "existing persistence is not broken",
      "all new tests pass"
    ]
  },
  {
    "number": 35,
    "title": "Session Start Flow",
    "phase_role": "build a clean session start flow for new game vs resume, while staying within the MVP scope.",
    "goal": "Make starting and resuming play more coherent without adding multi-character era selection yet.",
    "goal_detail": "The original roadmap imagined character/timeline selection, but the current MVP is still Kvothe/University. Keep the flow honest to that reality.",
    "scope": [
      "Add a start menu flow in the REPL",
      "Support new game vs resume",
      "Keep Kvothe/University as the only live new-game path",
      "Add tests where practical"
    ],
    "out_of_scope": [
      "Multiple playable characters",
      "Multi-era start options",
      "Fancy menu UI"
    ],
    "new_files": [
      "src/repl/startFlow.ts",
      "tests/startFlow.test.ts"
    ],
    "modified_files": [
      "src/repl.ts",
      "src/engine/saveLoadEngine.ts"
    ],
    "implementation_sections": "Keep the flow text-based and modest. Do not pretend multiple characters/eras exist if they do not.",
    "tests": "Add tests for new-game path, resume path, and missing-save fallback.",
    "success_criteria": [
      "the player can start a fresh game or resume",
      "the flow matches the real MVP scope",
      "all new tests pass"
    ]
  },
  {
    "number": 36,
    "title": "Integration Tests for Core MVP Loop",
    "phase_role": "write integration tests across movement, currency, tuition, and NPC talk for the current University MVP slice.",
    "goal": "Prove that the MVP loop works across systems, not just in isolated unit tests.",
    "goal_detail": "Use in-memory or controlled test state. Focus on realistic sequences.",
    "scope": [
      "Add an integration test suite",
      "Cover movement between seeded locations",
      "Cover tuition payment/deadline path",
      "Cover a simple NPC conversation path"
    ],
    "out_of_scope": [
      "Full playthrough automation",
      "Load testing",
      "Frontend testing"
    ],
    "new_files": [
      "tests/integration_mvp_loop.test.ts"
    ],
    "modified_files": [
      "tests/setup if needed"
    ],
    "implementation_sections": "Write sequence-based tests that simulate a few connected turns. Keep them readable.",
    "tests": "This prompt is itself the test-writing prompt. Cover movement, economy, tuition, and NPC talk in one slice.",
    "success_criteria": [
      "core MVP systems are proven together in at least one realistic sequence",
      "integration tests pass reliably"
    ]
  },
  {
    "number": 37,
    "title": "Tone Checker Harness",
    "phase_role": "build a lightweight tone-checking harness for sample narration outputs.",
    "goal": "Create a repeatable way to review whether narration drifts into modern idiom, gamey phrasing, or non-Kingkiller tone.",
    "goal_detail": "Keep it light. This is a review aid, not a production blocker yet.",
    "scope": [
      "Add a small tone checker harness or prompt artifact",
      "Support batch checking of sample outputs",
      "Focus on modern idiom and gamey language flags"
    ],
    "out_of_scope": [
      "Full automated literary evaluation",
      "Production moderation systems"
    ],
    "new_files": [
      "tools/tone_check_prompt.md",
      "tests/tone_check_samples.md"
    ],
    "modified_files": [
      "documentation only if needed"
    ],
    "implementation_sections": "Treat this as a workflow tool. You can generate a reusable review prompt and a sample bundle file.",
    "tests": "No code tests required unless you add helper scripts. The deliverable is the harness artifact.",
    "success_criteria": [
      "there is a repeatable tone-review workflow artifact",
      "the team can review narration samples consistently"
    ]
  },
  {
    "number": 38,
    "title": "Lore Guard Adversarial Tests",
    "phase_role": "stress-test the lore guard with adversarial mystery-extraction attempts.",
    "goal": "Verify that the structured mystery/lore guard holds under more aggressive prompts.",
    "goal_detail": "This is not about gameplay — it is about trust in the narration safety layer.",
    "scope": [
      "Add adversarial lore guard tests",
      "Use multiple forbidden-mystery phrasings",
      "Verify fallback behaviour"
    ],
    "out_of_scope": [
      "New lore systems",
      "New provider integrations"
    ],
    "new_files": [
      "tests/lore_guard_adversarial.test.ts"
    ],
    "modified_files": [
      "tests/loreGuard.test.ts"
    ],
    "implementation_sections": "Test several phrasings and indirect tries. Keep the system deterministic and conservative.",
    "tests": "This prompt is itself the test-writing prompt. Add multiple adversarial inputs and confirm they are blocked.",
    "success_criteria": [
      "the lore guard catches stronger extraction attempts",
      "fallback behaviour still works",
      "tests pass reliably"
    ]
  },
  {
    "number": 39,
    "title": "Random Turn Stability Test",
    "phase_role": "simulate many valid turns to look for crashes, state corruption, or broken exits.",
    "goal": "Find brittle spots in the deterministic engine before the MVP grows further.",
    "goal_detail": "Keep this practical and bounded rather than building a full fuzzing framework.",
    "scope": [
      "Add a random-valid-turn simulation test",
      "Run a controlled sequence of many turns",
      "Assert no crashes and sane state bounds"
    ],
    "out_of_scope": [
      "Formal property-based testing frameworks",
      "Massive fuzzing infrastructure"
    ],
    "new_files": [
      "tests/random_turn_stability.test.ts"
    ],
    "modified_files": [
      "none unless tiny test helpers are needed"
    ],
    "implementation_sections": "Use a seeded pseudo-random source or deterministic list so failures are repeatable. Check money/condition/reputation bounds and valid locations.",
    "tests": "This prompt is itself the test-writing prompt.",
    "success_criteria": [
      "the engine survives many turns without corruption",
      "state remains within expected bounds",
      "tests are repeatable"
    ]
  },
  {
    "number": 40,
    "title": "Full University Morning Pass",
    "phase_role": "do a full pass on one believable University morning and early evening loop, fixing coherence gaps.",
    "goal": "Turn the MVP from a pile of systems into one connected slice that feels playable.",
    "goal_detail": "Focus on coherence, not feature expansion. Wake, eat, class/social movement, work or study, and an evening music possibility should all feel like one world.",
    "scope": [
      "Run a scripted slice through the current MVP",
      "Note and fix coherence gaps",
      "Tighten small inconsistencies in status, prompts, and action flow"
    ],
    "out_of_scope": [
      "New major systems",
      "New regions",
      "Frontend work"
    ],
    "new_files": [
      "docs/full_pass_university_morning.md"
    ],
    "modified_files": [
      "small targeted fixes across existing files as needed"
    ],
    "implementation_sections": "Use this prompt as a disciplined polish pass. Fix only coherence and integration issues discovered during the slice. Keep changes small and justified.",
    "tests": "Add or update tests only where a discovered bug needs coverage.",
    "success_criteria": [
      "one University morning-to-evening slice plays coherently",
      "small integration gaps are fixed without feature sprawl",
      "the MVP feels more like a real game than a system bundle"
    ]
  }
]


4. project_state.md
This is the context file the generator injects into every prompt.
Prompts 1–20 are designed around a University-era Kvothe MVP.


Current architecture principles:
- engine owns truth
- narration never mutates state
- local deterministic path must always work
- optional Gemini path must fall back safely
- canon registry and mystery registry constrain narration
- no deep mystery resolution
- no refactors unless strictly required


Current MVP systems already planned:
- movement and location traversal
- day/time progression
- SQLite-backed player/world state
- hunger and fatigue
- tuition
- economy and currency
- inventory
- sympathy
- rumours
- Eolian / pipes
- busking at Anker’s
- reputation axes and NPC trust
- optional Gemini narration and NPC conversation
- lore guard


Current MVP focus:
- one playable era
- Kvothe only
- University + Imre slice
- grounded survival, study, work, and social consequence


5. hard_bans.md
No Anthropic. No OpenAI. No new AI provider.
Gemini remains the only optional LLM provider in this codebase.


No web server. No frontend unless a later prompt explicitly introduces it.
No random feature sprawl.
No major refactors.
Do not invent lore.
Do not resolve mysteries.
Do not add systems outside the prompt’s stated scope.
If a referenced file does not yet exist, skip it rather than inventing around it.


6. architecture_rule.md
engine/* owns:
- command parsing
- movement
- access checks
- time advancement
- state mutation
- persistence
- gameplay truth


content/* owns:
- authored registries and text pools only


narration/* owns:
- prose rendering only
- prompt construction only
- fallback rendering only
- canon and mystery safety only


Narration must never mutate PlayerState.
Narration must never write to the DB.
The engine remains the source of truth.
repl.ts saves state exactly once per turn.


7. architecture_reminders.md
These rules from Prompt 1 still apply:


- narration/* never reads from or writes to the DB
- narration/* never mutates PlayerState
- engine/* owns all state changes
- repl.ts saves state exactly once per turn
- No `any` types
- No external API calls outside the optional Gemini provider
- No unused imports or files
- Add tests alongside implementation


8. How to run it
mkdir -p prompt-gen/generated_prompts
cd prompt-gen


python3 generate_prompts.py --start 21 --end 25
python3 generate_prompts.py --start 26 --end 30
python3 generate_prompts.py --start 31 --end 35
python3 generate_prompts.py --start 36 --end 40


Or generate only selected prompts:
python3 generate_prompts.py --only 21,22,24


Or preview without writing files:
python3 generate_prompts.py --start 21 --end 23 --dry-run


9. Recommended workflow
Use it in blocks of 5.
That matches the roadmap structure much better than trying to auto-generate all 20 at once:
* 21–25 consequences, spread, Archives, sympathy, events
* 26–30 tuition hearing, Eolian audition, world state, Ambrose escalation, calendar
* 31–35 CLI polish and save/load
* 36–40 integration tests, tone/lore review, stability, full pass.
10. One important improvement
Before generating, I’d add a tiny reviewer script or checklist that asks:
* does this prompt duplicate an existing system?
* does it introduce a new system outside scope?
* does it sneak in a refactor?
* does it assume files that do not exist yet?
* does it match the original roadmap slot?
That catches a lot of drift early.
If you want, next I can give you a validator script that checks the manifest for prompt drift and missing fields before generation.
Yes — basically, yes. Codex is meant to work from a prompt/spec inside your IDE or terminal, navigate your repo, create/edit files, and run commands, and OpenAI’s docs say you can pair with it in VS Code, the terminal, or the Codex app.
But there’s one important catch:
What you’ve got here is a prompt-generator scaffold, not a full autonomous prompt-writer.
If you feed this to Codex, it should be able to make you a working generator that:
* creates the files
* writes generate_prompts.py
* writes prompt_template.md
* writes prompt_manifest_21_40.json
* writes the context files
* runs the script to generate prompt markdown files
That part is realistic. Codex is explicitly built to take a spec, edit files, and execute commands/tests in your environment.
What it will not magically do, unless you ask for a second layer, is:
* invent high-quality implementation_sections for all prompts 21–40 on its own
* guarantee the prompts stay aligned with your roadmap
* validate prompt drift unless you also ask it to build that reviewer/validator layer
So the clean answer is:
What Codex can do from what you pasted
It can build the generator system.
What still needs either you or another AI pass
The manifest content quality.
The script renders whatever is in the manifest. If the manifest is good, the generated prompts will be good. If the manifest is thin, the generated prompts will be thin.
Best way to use Codex in VS Code on WSL
Create an empty folder, then give Codex one tight task like this:
Create a local prompt generator project in this folder for my Kingkiller Chronicle game prompts 21–40.


Create these files exactly:
- generate_prompts.py
- prompt_template.md
- prompt_manifest_21_40.json
- project_state.md
- hard_bans.md
- architecture_rule.md
- architecture_reminders.md


Use the content I pasted below exactly unless a small fix is needed to make it run.


Then:
1. save all files
2. run the generator in dry-run mode for prompts 21–23
3. fix any errors
4. run it again and write the generated files into ./generated_prompts
5. show me a short summary of what was created and any issues found


Do not redesign the system.
Do not add dependencies.
Do not replace the manifest structure.
Only make minimal fixes needed for correctness.


Then paste the file contents underneath that.
Even better
Ask Codex to do it in two passes:
Pass 1
“Create the generator exactly from these files and make it run.”
Pass 2
“Now add a validator script that checks manifest entries for missing fields, duplicate prompt numbers, empty sections, and scope drift warnings.”
That will be more reliable than one huge all-in-one ask.
One thing I’d strongly recommend
Do not ask Codex to both:
* build the generator
* and auto-author all 20 prompts from scratch
in the same step.
That is where drift starts.
Better flow:
1. Codex builds the generator
2. You test it
3. Then either you or Codex fills the manifest for 21–25
4. Generate those
5. Review
6. Repeat for 26–30, and so on
Practical reality
If you paste all of this into Codex in VS Code on WSL, the likely result is:
* Yes: it can make the generator project
* Yes: it can probably run and fix it
* No: that alone does not guarantee great prompts 21–40
* Yes: it gives you a solid automation pipeline for generating them consistently
The most honest one-line summary
Codex can build you the prompt generator from this, but the manifest still controls the quality of the generated prompts.
If you want, I can now give you a single paste-ready Codex prompt that tells it exactly how to build this generator in your WSL folder.
Yes. If you paste a clear build spec into Codex in VS Code on WSL, it can create the files, edit them, run commands, and execute tests in your repo. That is exactly the local IDE/terminal workflow OpenAI describes for Codex.
What you want Codex to build is a prompt-generator project with:
* a Python generator script
* a template file
* a manifest file
* a few context files
* an output folder for generated prompts
Then you run it locally to generate prompts 21–40 in batches.
Here’s the single paste-ready Codex prompt:
Build a local prompt-generator project in this folder for my Kingkiller Chronicle text adventure prompts 21–40.


Environment:
- I am using VS Code on WSL
- Use Python 3 only
- No third-party dependencies
- Do not redesign the architecture
- Make the minimum changes needed for correctness and usability


Goal:
Create a working local generator that reads:
- prompt_template.md
- prompt_manifest_21_40.json
- optional context files:
  - project_state.md
  - hard_bans.md
  - architecture_rule.md
  - architecture_reminders.md


and writes generated prompt markdown files into:
- ./generated_prompts


Create these files exactly:
- generate_prompts.py
- prompt_template.md
- prompt_manifest_21_40.json
- project_state.md
- hard_bans.md
- architecture_rule.md
- architecture_reminders.md


Requirements:
1. The generator must:
   - validate the manifest
   - support:
     - --start
     - --end
     - --only
     - --dry-run
     - --out
   - create output filenames like:
     prompt_21_reputation_consequence_triggers.md
   - fail clearly on missing required manifest fields
   - render placeholders in the template using manifest + global context values


2. The template must use placeholders like:
   - {{number}}
   - {{previous_prompt}}
   - {{title}}
   - {{phase_role}}
   - {{goal}}
   - {{goal_detail}}
   - {{scope}}
   - {{out_of_scope}}
   - {{new_files}}
   - {{modified_files}}
   - {{implementation_sections}}
   - {{tests}}
   - {{success_criteria}}
   - {{hard_bans}}
   - {{architecture_rule}}
   - {{architecture_reminders}}
   - {{project_state}}


3. The manifest must include starter entries for prompts 21–40 based on this roadmap shape:
   - 21: reputation consequence triggers
   - 22: delayed reputation spread / “word travels”
   - 23: Archives access system
   - 24: basic sympathy action standardisation
   - 25: canon event system
   - 26: tuition hearing event
   - 27: first Eolian audition event framing
   - 28: world state manager
   - 29: Ambrose conflict escalation
   - 30: calendar and term structure
   - 31: CLI status/header polish
   - 32: command parsing expansion
   - 33: in-world help command
   - 34: save/load system
   - 35: session start flow
   - 36: integration tests for core MVP loop
   - 37: tone checker harness
   - 38: lore guard adversarial tests
   - 39: random turn stability test
   - 40: full University morning pass / coherence pass


4. Every manifest entry must include:
   - number
   - title
   - phase_role
   - goal
   - goal_detail
   - scope
   - out_of_scope
   - new_files
   - modified_files
   - implementation_sections
   - tests
   - success_criteria


5. The generated prompts must preserve these project rules:
   - engine owns truth
   - narration never mutates state
   - no deep mystery resolution
   - no major refactors
   - skip missing files rather than inventing around them
   - no new gameplay systems outside each prompt’s scope
   - all new tests must pass alongside earlier prompts


6. Also create sensible starter content for:
   - project_state.md
   - hard_bans.md
   - architecture_rule.md
   - architecture_reminders.md


7. After creating the files:
   - run the generator in dry-run mode for prompts 21–23
   - fix any errors
   - then generate prompts 21–25 into ./generated_prompts
   - show me a short summary of:
     - files created
     - commands run
     - any fixes made
     - the names of the generated prompt files


Important constraints:
- Do not add any package manager setup
- Do not add dependencies
- Do not create a web app
- Do not create a GUI
- Do not replace the manifest/template approach with a different system
- Keep the code readable and simple
- Use UTF-8 text files
- Comments should be brief and useful


Use this exact starter content structure unless a small correctness fix is needed:


1) generate_prompts.py
[PASTE THE FULL PYTHON SCRIPT HERE]


2) prompt_template.md
[PASTE THE FULL TEMPLATE HERE]


3) prompt_manifest_21_40.json
[PASTE THE FULL MANIFEST HERE]


4) project_state.md
Prompts 1–20 are designed around a University-era Kvothe MVP.


Current architecture principles:
- engine owns truth
- narration never mutates state
- local deterministic path must always work
- optional Gemini path must fall back safely
- canon registry and mystery registry constrain narration
- no deep mystery resolution
- no refactors unless strictly required


Current MVP systems already planned:
- movement and location traversal
- day/time progression
- SQLite-backed player/world state
- hunger and fatigue
- tuition
- economy and currency
- inventory
- sympathy
- rumours
- Eolian / pipes
- busking at Anker’s
- reputation axes and NPC trust
- optional Gemini narration and NPC conversation
- lore guard


Current MVP focus:
- one playable era
- Kvothe only
- University + Imre slice
- grounded survival, study, work, and social consequence


5) hard_bans.md
No Anthropic. No OpenAI. No new AI provider.
Gemini remains the only optional LLM provider in this codebase.


No web server. No frontend unless a later prompt explicitly introduces it.
No random feature sprawl.
No major refactors.
Do not invent lore.
Do not resolve mysteries.
Do not add systems outside the prompt’s stated scope.
If a referenced file does not yet exist, skip it rather than inventing around it.


6) architecture_rule.md
engine/* owns:
- command parsing
- movement
- access checks
- time advancement
- state mutation
- persistence
- gameplay truth


content/* owns:
- authored registries and text pools only


narration/* owns:
- prose rendering only
- prompt construction only
- fallback rendering only
- canon and mystery safety only


Narration must never mutate PlayerState.
Narration must never write to the DB.
The engine remains the source of truth.
repl.ts saves state exactly once per turn.


7) architecture_reminders.md
These rules from Prompt 1 still apply:


- narration/* never reads from or writes to the DB
- narration/* never mutates PlayerState
- engine/* owns all state changes
- repl.ts saves state exactly once per turn
- No `any` types
- No external API calls outside the optional Gemini provider
- No unused imports or files
- Add tests alongside implementation


Do the work now in this repo.


For the three [PASTE ... HERE] sections, paste in:
* the full generate_prompts.py
* the full prompt_template.md
* the full prompt_manifest_21_40.json
One important note: Codex should be able to build the generator from this prompt, but the quality of prompts 21–40 will still depend on the manifest content. The generator gives you consistency; the manifest gives you quality. That’s why generating in batches of 5 is the safer move.
If you want, next I’ll give you a single copy-paste block containing the full Python script + template + manifest so you can drop it straight into Codex without filling placeholders yourself.
Yep — here’s a single paste-ready block for Codex.
Paste the whole thing into Codex in your WSL folder.
Create a local prompt-generator project in this folder for my Kingkiller Chronicle text adventure prompts 21–40.


Environment:
- VS Code on WSL
- Python 3 only
- No third-party dependencies
- No redesigns unless needed for correctness
- Keep everything simple and readable


Goal:
Create these files exactly, with the exact contents provided below:
- generate_prompts.py
- prompt_template.md
- prompt_manifest_21_40.json
- project_state.md
- hard_bans.md
- architecture_rule.md
- architecture_reminders.md


Then:
1. save all files
2. run the generator in dry-run mode for prompts 21–23
3. fix any errors if needed
4. generate prompts 21–25 into ./generated_prompts
5. show me a short summary of:
   - files created
   - commands run
   - any fixes made
   - generated prompt filenames


Do not:
- add dependencies
- add a web app
- add a GUI
- replace the manifest/template approach
- invent extra files unless required for correctness


Use these exact file contents:


===== FILE: generate_prompts.py =====
#!/usr/bin/env python3
"""
Generate KKC prompt files from a JSON manifest and a markdown template.


No third-party dependencies required.
Uses:
- prompt_template.md
- prompt_manifest_21_40.json
- optional project_state.md
- optional hard_bans.md
- optional architecture_rule.md
- optional architecture_reminders.md


Outputs one .md file per prompt into ./generated_prompts by default.
"""


from __future__ import annotations


import argparse
import json
import re
from pathlib import Path
from typing import Any, Dict, Iterable, List, Optional




ROOT = Path(".")
DEFAULT_TEMPLATE = ROOT / "prompt_template.md"
DEFAULT_MANIFEST = ROOT / "prompt_manifest_21_40.json"
DEFAULT_OUTPUT_DIR = ROOT / "generated_prompts"


OPTIONAL_CONTEXT_FILES = {
    "project_state": ROOT / "project_state.md",
    "hard_bans": ROOT / "hard_bans.md",
    "architecture_rule": ROOT / "architecture_rule.md",
    "architecture_reminders": ROOT / "architecture_reminders.md",
}




REQUIRED_FIELDS = [
    "number",
    "title",
    "phase_role",
    "goal",
    "goal_detail",
    "scope",
    "out_of_scope",
    "new_files",
    "modified_files",
    "implementation_sections",
    "tests",
    "success_criteria",
]




def read_text(path: Path, default: str = "") -> str:
    return path.read_text(encoding="utf-8") if path.exists() else default




def normalise_newlines(text: str) -> str:
    return text.replace("\r\n", "\n").replace("\r", "\n").strip()




def format_bullets(items: Iterable[str], indent: str = "  - ") -> str:
    items = [str(x).strip() for x in items if str(x).strip()]
    return "\n".join(f"{indent}{item}" for item in items) if items else f"{indent}(none)"




def format_numbered(items: Iterable[str], indent: str = "  ") -> str:
    items = [str(x).strip() for x in items if str(x).strip()]
    return "\n".join(f"{indent}{i + 1}. {item}" for i, item in enumerate(items)) if items else f"{indent}1. (none)"




def slugify(text: str) -> str:
    text = text.lower().strip()
    text = re.sub(r"[^a-z0-9]+", "_", text)
    return re.sub(r"_+", "_", text).strip("_")




def validate_prompt_entry(entry: Dict[str, Any]) -> List[str]:
    errors: List[str] = []


    for field in REQUIRED_FIELDS:
        if field not in entry:
            errors.append(f"Missing required field: {field}")


    if "number" in entry and not isinstance(entry["number"], int):
        errors.append("Field 'number' must be an integer")


    list_fields = [
        "scope",
        "out_of_scope",
        "new_files",
        "modified_files",
        "success_criteria",
    ]
    for field in list_fields:
        if field in entry and not isinstance(entry[field], list):
            errors.append(f"Field '{field}' must be a list")


    string_fields = [
        "title",
        "phase_role",
        "goal",
        "goal_detail",
        "implementation_sections",
        "tests",
    ]
    for field in string_fields:
        if field in entry and not isinstance(entry[field], str):
            errors.append(f"Field '{field}' must be a string")


    return errors




def load_manifest(path: Path) -> List[Dict[str, Any]]:
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except FileNotFoundError:
        raise SystemExit(f"Manifest not found: {path}")
    except json.JSONDecodeError as exc:
        raise SystemExit(f"Manifest is not valid JSON: {exc}")


    if not isinstance(data, list):
        raise SystemExit("Manifest root must be a JSON array")


    errors: List[str] = []
    seen_numbers = set()


    for idx, entry in enumerate(data):
        if not isinstance(entry, dict):
            errors.append(f"Entry {idx} is not an object")
            continue


        entry_errors = validate_prompt_entry(entry)
        if entry_errors:
            errors.extend([f"Entry {idx}: {msg}" for msg in entry_errors])


        number = entry.get("number")
        if isinstance(number, int):
            if number in seen_numbers:
                errors.append(f"Duplicate prompt number: {number}")
            seen_numbers.add(number)


    if errors:
        joined = "\n".join(f"- {e}" for e in errors)
        raise SystemExit(f"Manifest validation failed:\n{joined}")


    return data




def render_template(template: str, values: Dict[str, str]) -> str:
    missing = []


    def replacer(match: re.Match[str]) -> str:
        key = match.group(1)
        if key not in values:
            missing.append(key)
            return match.group(0)
        return values[key]


    rendered = re.sub(r"\{\{([a-zA-Z0-9_]+)\}\}", replacer, template)


    if missing:
        missing_list = ", ".join(sorted(set(missing)))
        raise SystemExit(f"Template contains unresolved placeholders: {missing_list}")


    return rendered.strip() + "\n"




def build_values(
    entry: Dict[str, Any],
    global_context: Dict[str, str],
) -> Dict[str, str]:
    number = entry["number"]
    previous_prompt = str(number - 1)


    title = str(entry["title"]).strip()
    filename_slug = slugify(title)


    values: Dict[str, str] = {
        "number": str(number),
        "previous_prompt": previous_prompt,
        "title": title,
        "phase_role": str(entry["phase_role"]).strip(),
        "goal": str(entry["goal"]).strip(),
        "goal_detail": normalise_newlines(str(entry["goal_detail"])),
        "scope": format_numbered(entry["scope"]),
        "out_of_scope": format_bullets(entry["out_of_scope"]),
        "new_files": format_bullets(entry["new_files"]),
        "modified_files": format_bullets(entry["modified_files"]),
        "implementation_sections": normalise_newlines(str(entry["implementation_sections"])),
        "tests": normalise_newlines(str(entry["tests"])),
        "success_criteria": format_bullets(entry["success_criteria"]),
        "hard_bans": global_context["hard_bans"],
        "architecture_rule": global_context["architecture_rule"],
        "architecture_reminders": global_context["architecture_reminders"],
        "project_state": global_context["project_state"],
        "filename_slug": filename_slug,
    }


    return values




def select_entries(
    manifest: List[Dict[str, Any]],
    start: Optional[int],
    end: Optional[int],
    only: Optional[List[int]],
) -> List[Dict[str, Any]]:
    entries = manifest


    if only:
        wanted = set(only)
        entries = [e for e in entries if e["number"] in wanted]


    if start is not None:
        entries = [e for e in entries if e["number"] >= start]


    if end is not None:
        entries = [e for e in entries if e["number"] <= end]


    entries = sorted(entries, key=lambda e: e["number"])


    if not entries:
        raise SystemExit("No prompt entries selected")


    return entries




def parse_only_list(value: Optional[str]) -> Optional[List[int]]:
    if not value:
        return None
    nums = []
    for chunk in value.split(","):
        chunk = chunk.strip()
        if not chunk:
            continue
        try:
            nums.append(int(chunk))
        except ValueError:
            raise SystemExit(f"Invalid prompt number in --only: {chunk}")
    return nums or None




def build_global_context() -> Dict[str, str]:
    return {
        "project_state": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["project_state"], "")),
        "hard_bans": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["hard_bans"], "HARD BANS — UNCHANGED")),
        "architecture_rule": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["architecture_rule"], "ARCHITECTURE RULE — STILL ABSOLUTE")),
        "architecture_reminders": normalise_newlines(read_text(OPTIONAL_CONTEXT_FILES["architecture_reminders"], "These rules from Prompt 1 still apply.")),
    }




def main() -> None:
    parser = argparse.ArgumentParser(description="Generate KKC prompt markdown files from a manifest.")
    parser.add_argument("--template", type=Path, default=DEFAULT_TEMPLATE, help="Path to prompt template markdown")
    parser.add_argument("--manifest", type=Path, default=DEFAULT_MANIFEST, help="Path to JSON manifest")
    parser.add_argument("--out", type=Path, default=DEFAULT_OUTPUT_DIR, help="Output directory")
    parser.add_argument("--start", type=int, help="First prompt number to generate")
    parser.add_argument("--end", type=int, help="Last prompt number to generate")
    parser.add_argument("--only", type=str, help="Comma-separated prompt numbers to generate, e.g. 21,22,25")
    parser.add_argument("--dry-run", action="store_true", help="Print generated prompts instead of writing files")
    args = parser.parse_args()


    template = read_text(args.template)
    if not template:
        raise SystemExit(f"Template not found or empty: {args.template}")


    manifest = load_manifest(args.manifest)
    only = parse_only_list(args.only)
    entries = select_entries(manifest, args.start, args.end, only)
    global_context = build_global_context()


    if not args.dry_run:
        args.out.mkdir(parents=True, exist_ok=True)


    for entry in entries:
        values = build_values(entry, global_context)
        rendered = render_template(template, values)


        filename = f"prompt_{entry['number']:02}_{values['filename_slug']}.md"
        output_path = args.out / filename


        if args.dry_run:
            divider = "=" * 80
            print(divider)
            print(filename)
            print(divider)
            print(rendered)
        else:
            output_path.write_text(rendered, encoding="utf-8")
            print(f"Wrote {output_path}")




if __name__ == "__main__":
    main()


===== FILE: prompt_template.md =====
KKC TEXT ADVENTURE — PROMPT {{number}} OF 180
==========================================
{{title}}
==========================================


This prompt builds directly on Prompts 1 through {{previous_prompt}}.
Do not restructure, rename, or refactor anything from those prompts.
Only add to what exists.


This prompt follows the original Phase 1 plan role for Prompt {{number}}:
{{phase_role}}


Current project state:
{{project_state}}


═══════════════════════════════════════
HARD BANS — UNCHANGED
═══════════════════════════════════════


{{hard_bans}}


═══════════════════════════════════════
ARCHITECTURE RULE — STILL ABSOLUTE
═══════════════════════════════════════


{{architecture_rule}}


═══════════════════════════════════════
GOAL OF THIS PROMPT
═══════════════════════════════════════


{{goal}}


{{goal_detail}}


═══════════════════════════════════════
SCOPE OF THIS PROMPT
═══════════════════════════════════════


This prompt covers:
{{scope}}


This prompt does NOT cover:
{{out_of_scope}}


═══════════════════════════════════════
NEW FILES IN THIS PROMPT
═══════════════════════════════════════


Add exactly these files:
{{new_files}}


Modify these existing files:
{{modified_files}}


Do not add any other files.


═══════════════════════════════════════
IMPLEMENTATION DETAILS
═══════════════════════════════════════


{{implementation_sections}}


═══════════════════════════════════════
TESTS
═══════════════════════════════════════


{{tests}}


═══════════════════════════════════════
ARCHITECTURE REMINDERS
═══════════════════════════════════════


{{architecture_reminders}}


═══════════════════════════════════════
SUCCESS CRITERIA
═══════════════════════════════════════


This prompt is complete when:
{{success_criteria}}


==========================================
END OF PROMPT {{number}}
==========================================


===== FILE: prompt_manifest_21_40.json =====
[
  {
    "number": 21,
    "title": "Reputation Consequence Triggers",
    "phase_role": "write reputation consequence triggers so academic and social standing start affecting reactions and access in modest, deterministic ways.",
    "goal": "Introduce consequence triggers on top of the centralised reputation system without adding a huge new world-simulation layer.",
    "goal_detail": "Focus on small, testable consequences. Examples: low academic standing affects how certain Masters or academic-facing systems respond; very poor trust with Ambrose-related social channels can make specific interactions more hostile. Keep this modest and deterministic.",
    "scope": [
      "Add engine/reputationConsequences.ts",
      "Define trigger helpers for academic standing and selected NPC trust bands",
      "Wire modest consequence checks into existing relevant flows",
      "Add tests for trigger thresholds and non-trigger cases"
    ],
    "out_of_scope": [
      "Delayed reputation spread",
      "Dynamic NPC schedules",
      "Large-scale hostility systems",
      "New commands"
    ],
    "new_files": [
      "src/engine/reputationConsequences.ts",
      "tests/reputationConsequences.test.ts"
    ],
    "modified_files": [
      "src/engine/actions.ts",
      "src/engine/npcEngine.ts",
      "src/narration/renderStatus.ts"
    ],
    "implementation_sections": "Implement small, deterministic consequence helpers keyed off the shared reputation engine. Keep effects local and readable. Do not build a faction framework. Use threshold bands and explicit checks. If a consequence message is needed, keep it brief and in-world.",
    "tests": "Add tests for low academic standing trigger, no-trigger when standing is decent, and an Ambrose-trust-based social hostility check that remains local rather than global.",
    "success_criteria": [
      "reputation consequences are centralised and deterministic",
      "existing systems can query consequence state without duplicating threshold logic",
      "no delayed spread system is introduced yet",
      "all new tests pass"
    ]
  },
  {
    "number": 22,
    "title": "Word Travels: Pending Reputation Changes Queue",
    "phase_role": "add a delayed reputation spread queue so some changes reach the wider world after 1–3 in-game days instead of instantly.",
    "goal": "Make the world feel more socially believable by separating immediate internal changes from delayed wider awareness.",
    "goal_detail": "Keep the delayed queue narrow and mechanical. Immediate direct trust changes can remain immediate; broader public-facing reputation shifts can queue and apply later.",
    "scope": [
      "Add engine/reputationQueue.ts",
      "Use pending_reputation_changes table or existing persistence structure",
      "Apply queued changes when day advances",
      "Add tests for delay and one-time application"
    ],
    "out_of_scope": [
      "Large social simulation",
      "Dynamic rumour spread",
      "NPC memory systems",
      "New commands"
    ],
    "new_files": [
      "src/engine/reputationQueue.ts",
      "tests/reputationQueue.test.ts"
    ],
    "modified_files": [
      "src/engine/time.ts",
      "src/engine/socialEngine.ts",
      "src/engine/musicEngine.ts"
    ],
    "implementation_sections": "Implement a small pending queue abstraction. Queue public-facing changes with apply_on_day, then apply and clear them when the current day reaches that threshold. Keep ordering deterministic. Avoid background complexity.",
    "tests": "Add tests for queuing a change, not applying it early, applying it on the correct day, and not applying it twice.",
    "success_criteria": [
      "pending public reputation changes can be delayed",
      "queued changes apply on the correct day",
      "queued changes apply only once",
      "existing immediate systems still work"
    ]
  },
  {
    "number": 23,
    "title": "Archives Access System",
    "phase_role": "formalise Archives access as requiring Re'lar rank and Lorren's approval, with proper in-world denial handling.",
    "goal": "Turn the existing Archives restrictions into one clear system rather than scattered checks.",
    "goal_detail": "Use the canon-safe MVP rule already established: the Stacks require Re'lar and Lorren's approval. Keep denial grounded and institutional.",
    "scope": [
      "Add engine/archivesAccess.ts",
      "Track Lorren approval in world/player state if needed",
      "Centralise access checks and denial text",
      "Add tests for rank and approval combinations"
    ],
    "out_of_scope": [
      "Full Archives browsing",
      "Lorren as a full active NPC system",
      "Research mechanics",
      "New map areas beyond current Archives spaces"
    ],
    "new_files": [
      "src/engine/archivesAccess.ts",
      "tests/archivesAccess.test.ts"
    ],
    "modified_files": [
      "src/types/index.ts",
      "src/engine/movement.ts",
      "src/narration/renderLocation.ts"
    ],
    "implementation_sections": "Create one Archives access helper that determines whether the player may enter the Stacks and returns a clean in-world failure line when blocked. Reuse existing rank and canon registry truth.",
    "tests": "Add tests for blocked E'lir, blocked Re'lar without approval if approval tracking exists, and successful access when all conditions are met.",
    "success_criteria": [
      "Archives access logic is centralised",
      "the Stacks require Re'lar and approval",
      "denial messages are grounded and consistent",
      "movement/rendering respect the shared access logic"
    ]
  },
  {
    "number": 24,
    "title": "Basic Sympathy Action Standardisation",
    "phase_role": "turn the early sympathy system into one clear MVP action path with deterministic pass/fail and consistent consequences.",
    "goal": "Consolidate sympathy action handling so later prompts can build on a stable core.",
    "goal_detail": "Use the systems already introduced: links, warmth/body heat, materials, and simple backlash/slip outcomes. This is not full naming or advanced sympathy depth yet.",
    "scope": [
      "Add engine/sympathyAction.ts or equivalent central module",
      "Route existing sympathy use through shared helpers",
      "Standardise consequences and result text hooks",
      "Add tests for successful, failed, and unsafe actions"
    ],
    "out_of_scope": [
      "Advanced sympathy chaining",
      "Naming",
      "Alchemy",
      "Crafting"
    ],
    "new_files": [
      "src/engine/sympathyAction.ts",
      "tests/sympathyAction.test.ts"
    ],
    "modified_files": [
      "src/engine/actions.ts",
      "src/engine/sympathyEngine.ts",
      "src/narration/narrationContext.ts"
    ],
    "implementation_sections": "Keep the logic deterministic and compact. The engine decides if the action works, what it costs, and what consequence bucket it lands in. The narration layer only expresses it.",
    "tests": "Add tests for a valid action, a blocked action due to missing material or unsafe condition, and a failed action with an appropriate consequence bucket.",
    "success_criteria": [
      "sympathy action handling is centralised",
      "the engine owns all success/failure truth",
      "later prompts can build on this shared path",
      "all new tests pass"
    ]
  },
  {
    "number": 25,
    "title": "Canon Event System",
    "phase_role": "add a small event system where fixed or semi-fixed events can trigger from world conditions without railroading the player.",
    "goal": "Create the structural layer needed for tuition hearing, first Eolian audition framing, and later University events.",
    "goal_detail": "Keep the event system modest: trigger conditions, fixed vs flexible framing, and world state changes. Do not explode into branching narrative infrastructure.",
    "scope": [
      "Add engine/eventEngine.ts",
      "Define event shape and trigger condition checks",
      "Add world-state updates on event resolution",
      "Add tests for trigger evaluation"
    ],
    "out_of_scope": [
      "Large branching story graphs",
      "Cinematic scripting",
      "Multi-era story management",
      "Frontend event UI"
    ],
    "new_files": [
      "src/engine/eventEngine.ts",
      "tests/eventEngine.test.ts"
    ],
    "modified_files": [
      "src/types/index.ts",
      "src/engine/actions.ts",
      "src/engine/state.ts"
    ],
    "implementation_sections": "Implement a compact event model with id, trigger_conditions, is_fixed, and world_state_changes. Check triggers after actions. Keep it deterministic and engine-owned.",
    "tests": "Add tests for unmet triggers, met triggers, single-fire events, and world-state updates.",
    "success_criteria": [
      "events can trigger from world conditions",
      "world_state changes are applied deterministically",
      "the system is ready for concrete Phase 1 events",
      "all new tests pass"
    ]
  },
  {
    "number": 26,
    "title": "Tuition Hearing Event",
    "phase_role": "script the tuition hearing as a canon-grounded fixed event whose framing can vary but whose institutional reality remains intact.",
    "goal": "Use the event system to make tuition feel like a real University pressure point.",
    "goal_detail": "Keep the hearing modest and text-centric. The player can affect approach and flavour, but the event remains institutionally bounded.",
    "scope": [
      "Add authored tuition hearing event definition",
      "Tie it to tuition and term conditions",
      "Add outcome hooks for academic standing/social pressure",
      "Add tests for trigger and outcome application"
    ],
    "out_of_scope": [
      "Full Masters dialogue roster",
      "Large branching tribunal scenes",
      "Rank advancement decisions"
    ],
    "new_files": [
      "src/content/events/tuitionHearing.ts",
      "tests/tuitionHearingEvent.test.ts"
    ],
    "modified_files": [
      "src/engine/eventEngine.ts",
      "src/engine/tuitionEngine.ts"
    ],
    "implementation_sections": "Use the event system from Prompt 25. Keep the event fixed in nature but allow small flavour variations based on state. Do not over-script every Master yet.",
    "tests": "Add tests for correct trigger conditions and correct world/reputation changes after the event.",
    "success_criteria": [
      "the tuition hearing can trigger correctly",
      "the event feels institutional and grounded",
      "the system remains deterministic",
      "all new tests pass"
    ]
  },
  {
    "number": 27,
    "title": "First Eolian Audition Event Framing",
    "phase_role": "script the first Eolian audition as a proper event wrapper around the existing music logic.",
    "goal": "Make the first serious attempt for pipes feel like an event, not just a mechanical function call.",
    "goal_detail": "Use the existing audition logic and event system together. The player’s prep and condition matter, but the structure remains controlled.",
    "scope": [
      "Add an Eolian audition event definition",
      "Use existing music/audition truth",
      "Add event framing and world-state updates",
      "Add tests"
    ],
    "out_of_scope": [
      "New music mechanics",
      "Denna scene scripting",
      "Concert-style multi-stage event trees"
    ],
    "new_files": [
      "src/content/events/eolianFirstAudition.ts",
      "tests/eolianFirstAuditionEvent.test.ts"
    ],
    "modified_files": [
      "src/engine/eventEngine.ts",
      "src/engine/musicEngine.ts"
    ],
    "implementation_sections": "Wrap the existing audition logic rather than replacing it. The event layer should frame the moment and update world state, but not own music truth.",
    "tests": "Add tests for trigger conditions, successful world-state update, and correct use of underlying audition truth.",
    "success_criteria": [
      "the first audition can be framed as an event",
      "the event uses existing music truth rather than duplicating it",
      "world state can record that it happened",
      "all new tests pass"
    ]
  },
  {
    "number": 28,
    "title": "World State Manager",
    "phase_role": "formalise world state tracking so flags, term context, and event markers live in one coherent place.",
    "goal": "Make world_state a first-class shared structure rather than a loose collection of flags.",
    "goal_detail": "Focus on coherent access/update helpers, not on adding lots of new flags.",
    "scope": [
      "Add engine/worldStateEngine.ts",
      "Centralise get/set/update helpers",
      "Standardise common Phase 1 flags",
      "Add tests for state mutation and retrieval"
    ],
    "out_of_scope": [
      "Save-slot systems",
      "Branch visualisation",
      "Narrative debugging UI"
    ],
    "new_files": [
      "src/engine/worldStateEngine.ts",
      "tests/worldStateEngine.test.ts"
    ],
    "modified_files": [
      "src/engine/state.ts",
      "src/engine/eventEngine.ts"
    ],
    "implementation_sections": "Keep helpers small and deterministic. This is a coherence pass for flags like has_met_X, term_number, days_until_tuition, event markers, and access markers.",
    "tests": "Add tests for world-state get/set/update behaviour and compatibility with persisted player/world state.",
    "success_criteria": [
      "world_state has a shared management layer",
      "flags become easier to reason about",
      "event and progression systems can rely on one shared place",
      "all new tests pass"
    ]
  },
  {
    "number": 29,
    "title": "Ambrose Conflict Escalation",
    "phase_role": "turn Ambrose pressure into a staged escalation system rather than isolated incidents.",
    "goal": "Make Ambrose feel like a persistent social threat without overbuilding a villain system.",
    "goal_detail": "Use small, deterministic stages. This is still University-level tension, not later-book-scale plotting.",
    "scope": [
      "Add ambroseEscalation.ts",
      "Track escalation stage",
      "Tie stage changes to social interactions and trust bands",
      "Add tests for escalation progression"
    ],
    "out_of_scope": [
      "Sabotage scenes",
      "Theft plots",
      "Plum bob",
      "Major disciplinary hearings"
    ],
    "new_files": [
      "src/engine/ambroseEscalation.ts",
      "tests/ambroseEscalation.test.ts"
    ],
    "modified_files": [
      "src/engine/socialEngine.ts",
      "src/engine/worldStateEngine.ts"
    ],
    "implementation_sections": "Use a small stage model, e.g. quiet hostility -> public cuttingness -> formal trouble pressure. Keep consequences local and deterministic.",
    "tests": "Add tests for stage progression, no-jump behaviour, and stable persistence.",
    "success_criteria": [
      "Ambrose conflict now escalates coherently",
      "the escalation remains modest and Phase-1 appropriate",
      "world state can remember the stage",
      "all new tests pass"
    ]
  },
  {
    "number": 30,
    "title": "Calendar and Term Structure",
    "phase_role": "add a simple calendar so days, weeks, and term framing become explicit and deadlines sit on a real timeline.",
    "goal": "Make time feel like a University term rather than just a day counter.",
    "goal_detail": "Keep the calendar simple and mechanical. This is for deadline realism, not for seasonal simulation.",
    "scope": [
      "Add engine/calendarEngine.ts",
      "Map day numbers into weeks/term context",
      "Expose helpers for deadline/date text",
      "Add tests"
    ],
    "out_of_scope": [
      "Season/weather simulation",
      "Holiday scheduling",
      "Multi-term generation"
    ],
    "new_files": [
      "src/engine/calendarEngine.ts",
      "tests/calendarEngine.test.ts"
    ],
    "modified_files": [
      "src/narration/renderStatus.ts",
      "src/engine/tuitionEngine.ts"
    ],
    "implementation_sections": "Implement a light calendar abstraction around the existing day_number/term_number rather than replacing them. Keep it deterministic and useful for status/deadlines.",
    "tests": "Add tests for week/day mapping and stable deadline text generation.",
    "success_criteria": [
      "the game can express time as part of a term structure",
      "deadline systems can refer to the calendar cleanly",
      "existing day tracking remains compatible",
      "all new tests pass"
    ]
  },
  {
    "number": 31,
    "title": "CLI Status Header",
    "phase_role": "improve the REPL interface so each turn surfaces location, time, and compact condition info more cleanly.",
    "goal": "Make the terminal experience more readable without changing underlying systems.",
    "goal_detail": "This is a usability/presentation pass, not a frontend rewrite.",
    "scope": [
      "Add a reusable status header renderer",
      "Show location/time/condition summary above narration blocks",
      "Keep the CLI compact",
      "Add tests where practical"
    ],
    "out_of_scope": [
      "Web UI",
      "Rich text formatting",
      "Maps or side panels"
    ],
    "new_files": [
      "src/narration/renderHeader.ts",
      "tests/renderHeader.test.ts"
    ],
    "modified_files": [
      "src/repl.ts",
      "src/narration/renderStatus.ts"
    ],
    "implementation_sections": "Keep the interface plain and readable. Use one compact header block before scene text. Avoid vertical bloat.",
    "tests": "Add snapshot-style or string-shape tests for compact header output.",
    "success_criteria": [
      "the CLI shows a cleaner status header",
      "readability improves without changing systems",
      "all new tests pass"
    ]
  },
  {
    "number": 32,
    "title": "Command Parsing Expansion",
    "phase_role": "expand command parsing to support the wider MVP verb set cleanly and deterministically.",
    "goal": "Consolidate the growing command surface so it stays readable and testable.",
    "goal_detail": "The verbs are already emerging across prompts. This prompt makes parsing consistent rather than adding whole new mechanics.",
    "scope": [
      "Standardise command parsing patterns",
      "Support existing verbs cleanly",
      "Improve parser tests",
      "Keep logic deterministic"
    ],
    "out_of_scope": [
      "Natural-language free parsing",
      "LLM command understanding",
      "New core mechanics"
    ],
    "new_files": [
      "src/engine/commandParser.ts",
      "tests/commandParser.test.ts"
    ],
    "modified_files": [
      "src/engine/actions.ts",
      "src/engine/npcEngine.ts"
    ],
    "implementation_sections": "Extract the repetitive command matching into a central parser if and only if that reduces duplication without a large refactor. Keep command families explicit.",
    "tests": "Add tests for go/talk/ask/examine/use/listen/status/help and ambiguous edge cases.",
    "success_criteria": [
      "command parsing is more coherent",
      "existing commands still work",
      "no freeform parser overreach is introduced",
      "all new tests pass"
    ]
  },
  {
    "number": 33,
    "title": "In-World Help Command",
    "phase_role": "improve the help command so it remains useful without breaking immersion.",
    "goal": "Make help clearer, context-aware where practical, and still in-world.",
    "goal_detail": "Use the command set that actually exists by this point. Keep the tone natural and concise.",
    "scope": [
      "Centralise help text rendering",
      "Reflect currently available command families",
      "Keep tone in-world",
      "Add tests"
    ],
    "out_of_scope": [
      "Tutorial mode",
      "Interactive onboarding flow",
      "Context-sensitive walkthroughs"
    ],
    "new_files": [
      "src/narration/renderHelp.ts",
      "tests/renderHelp.test.ts"
    ],
    "modified_files": [
      "src/narration/localNarrator.ts",
      "src/engine/actions.ts"
    ],
    "implementation_sections": "Turn help into a maintained renderer rather than scattered strings. Keep it short and useful.",
    "tests": "Add tests for output containing the current major verb families without using gamey language.",
    "success_criteria": [
      "help remains in-world and useful",
      "the command set is represented accurately",
      "all new tests pass"
    ]
  },
  {
    "number": 34,
    "title": "Save and Load System",
    "phase_role": "implement save/load so player_state and world_state can persist beyond the SQLite session if needed.",
    "goal": "Add explicit save/load files on top of the existing persistence model.",
    "goal_detail": "Keep the format simple and transparent. This is about reliability, not multiple save-slot UX.",
    "scope": [
      "Add saveLoadEngine.ts",
      "Serialise player_state and world_state to JSON",
      "Support save and load helpers",
      "Add tests for round-trip correctness"
    ],
    "out_of_scope": [
      "Multiple save slots",
      "Cloud sync",
      "Encryption",
      "UI save menus"
    ],
    "new_files": [
      "src/engine/saveLoadEngine.ts",
      "tests/saveLoadEngine.test.ts"
    ],
    "modified_files": [
      "src/repl.ts",
      "src/engine/state.ts"
    ],
    "implementation_sections": "Use plain JSON files with clear structure. Keep save/load explicit and testable. Do not replace the SQLite backing store; layer on top carefully.",
    "tests": "Add tests for save file creation, load round-trip, and graceful handling of missing files.",
    "success_criteria": [
      "state can be serialised and loaded cleanly",
      "round-trip preserves core fields",
      "existing persistence is not broken",
      "all new tests pass"
    ]
  },
  {
    "number": 35,
    "title": "Session Start Flow",
    "phase_role": "build a clean session start flow for new game vs resume, while staying within the MVP scope.",
    "goal": "Make starting and resuming play more coherent without adding multi-character era selection yet.",
    "goal_detail": "The original roadmap imagined character/timeline selection, but the current MVP is still Kvothe/University. Keep the flow honest to that reality.",
    "scope": [
      "Add a start menu flow in the REPL",
      "Support new game vs resume",
      "Keep Kvothe/University as the only live new-game path",
      "Add tests where practical"
    ],
    "out_of_scope": [
      "Multiple playable characters",
      "Multi-era start options",
      "Fancy menu UI"
    ],
    "new_files": [
      "src/repl/startFlow.ts",
      "tests/startFlow.test.ts"
    ],
    "modified_files": [
      "src/repl.ts",
      "src/engine/saveLoadEngine.ts"
    ],
    "implementation_sections": "Keep the flow text-based and modest. Do not pretend multiple characters/eras exist if they do not.",
    "tests": "Add tests for new-game path, resume path, and missing-save fallback.",
    "success_criteria": [
      "the player can start a fresh game or resume",
      "the flow matches the real MVP scope",
      "all new tests pass"
    ]
  },
  {
    "number": 36,
    "title": "Integration Tests for Core MVP Loop",
    "phase_role": "write integration tests across movement, currency, tuition, and NPC talk for the current University MVP slice.",
    "goal": "Prove that the MVP loop works across systems, not just in isolated unit tests.",
    "goal_detail": "Use in-memory or controlled test state. Focus on realistic sequences.",
    "scope": [
      "Add an integration test suite",
      "Cover movement between seeded locations",
      "Cover tuition payment/deadline path",
      "Cover a simple NPC conversation path"
    ],
    "out_of_scope": [
      "Full playthrough automation",
      "Load testing",
      "Frontend testing"
    ],
    "new_files": [
      "tests/integration_mvp_loop.test.ts"
    ],
    "modified_files": [
      "tests/setup if needed"
    ],
    "implementation_sections": "Write sequence-based tests that simulate a few connected turns. Keep them readable.",
    "tests": "This prompt is itself the test-writing prompt. Cover movement, economy, tuition, and NPC talk in one slice.",
    "success_criteria": [
      "core MVP systems are proven together in at least one realistic sequence",
      "integration tests pass reliably"
    ]
  },
  {
    "number": 37,
    "title": "Tone Checker Harness",
    "phase_role": "build a lightweight tone-checking harness for sample narration outputs.",
    "goal": "Create a repeatable way to review whether narration drifts into modern idiom, gamey phrasing, or non-Kingkiller tone.",
    "goal_detail": "Keep it light. This is a review aid, not a production blocker yet.",
    "scope": [
      "Add a small tone checker harness or prompt artifact",
      "Support batch checking of sample outputs",
      "Focus on modern idiom and gamey language flags"
    ],
    "out_of_scope": [
      "Full automated literary evaluation",
      "Production moderation systems"
    ],
    "new_files": [
      "tools/tone_check_prompt.md",
      "tests/tone_check_samples.md"
    ],
    "modified_files": [
      "documentation only if needed"
    ],
    "implementation_sections": "Treat this as a workflow tool. You can generate a reusable review prompt and a sample bundle file.",
    "tests": "No code tests required unless you add helper scripts. The deliverable is the harness artifact.",
    "success_criteria": [
      "there is a repeatable tone-review workflow artifact",
      "the team can review narration samples consistently"
    ]
  },
  {
    "number": 38,
    "title": "Lore Guard Adversarial Tests",
    "phase_role": "stress-test the lore guard with adversarial mystery-extraction attempts.",
    "goal": "Verify that the structured mystery/lore guard holds under more aggressive prompts.",
    "goal_detail": "This is not about gameplay — it is about trust in the narration safety layer.",
    "scope": [
      "Add adversarial lore guard tests",
      "Use multiple forbidden-mystery phrasings",
      "Verify fallback behaviour"
    ],
    "out_of_scope": [
      "New lore systems",
      "New provider integrations"
    ],
    "new_files": [
      "tests/lore_guard_adversarial.test.ts"
    ],
    "modified_files": [
      "tests/loreGuard.test.ts"
    ],
    "implementation_sections": "Test several phrasings and indirect tries. Keep the system deterministic and conservative.",
    "tests": "This prompt is itself the test-writing prompt. Add multiple adversarial inputs and confirm they are blocked.",
    "success_criteria": [
      "the lore guard catches stronger extraction attempts",
      "fallback behaviour still works",
      "tests pass reliably"
    ]
  },
  {
    "number": 39,
    "title": "Random Turn Stability Test",
    "phase_role": "simulate many valid turns to look for crashes, state corruption, or broken exits.",
    "goal": "Find brittle spots in the deterministic engine before the MVP grows further.",
    "goal_detail": "Keep this practical and bounded rather than building a full fuzzing framework.",
    "scope": [
      "Add a random-valid-turn simulation test",
      "Run a controlled sequence of many turns",
      "Assert no crashes and sane state bounds"
    ],
    "out_of_scope": [
      "Formal property-based testing frameworks",
      "Massive fuzzing infrastructure"
    ],
    "new_files": [
      "tests/random_turn_stability.test.ts"
    ],
    "modified_files": [
      "none unless tiny test helpers are needed"
    ],
    "implementation_sections": "Use a seeded pseudo-random source or deterministic list so failures are repeatable. Check money, condition, reputation bounds, and valid locations.",
    "tests": "This prompt is itself the test-writing prompt.",
    "success_criteria": [
      "the engine survives many turns without corruption",
      "state remains within expected bounds",
      "tests are repeatable"
    ]
  },
  {
    "number": 40,
    "title": "Full University Morning Pass",
    "phase_role": "do a full pass on one believable University morning and early evening loop, fixing coherence gaps.",
    "goal": "Turn the MVP from a pile of systems into one connected slice that feels playable.",
    "goal_detail": "Focus on coherence, not feature expansion. Wake, eat, class/social movement, work or study, and an evening music possibility should all feel like one world.",
    "scope": [
      "Run a scripted slice through the current MVP",
      "Note and fix coherence gaps",
      "Tighten small inconsistencies in status, prompts, and action flow"
    ],
    "out_of_scope": [
      "New major systems",
      "New regions",
      "Frontend work"
    ],
    "new_files": [
      "docs/full_pass_university_morning.md"
    ],
    "modified_files": [
      "small targeted fixes across existing files as needed"
    ],
    "implementation_sections": "Use this prompt as a disciplined polish pass. Fix only coherence and integration issues discovered during the slice. Keep changes small and justified.",
    "tests": "Add or update tests only where a discovered bug needs coverage.",
    "success_criteria": [
      "one University morning-to-evening slice plays coherently",
      "small integration gaps are fixed without feature sprawl",
      "the MVP feels more like a real game than a system bundle"
    ]
  }
]


===== FILE: project_state.md =====
Prompts 1–20 are designed around a University-era Kvothe MVP.


Current architecture principles:
- engine owns truth
- narration never mutates state
- local deterministic path must always work
- optional Gemini path must fall back safely
- canon registry and mystery registry constrain narration
- no deep mystery resolution
- no refactors unless strictly required


Current MVP systems already planned:
- movement and location traversal
- day/time progression
- SQLite-backed player/world state
- hunger and fatigue
- tuition
- economy and currency
- inventory
- sympathy
- rumours
- Eolian / pipes
- busking at Anker’s
- reputation axes and NPC trust
- optional Gemini narration and NPC conversation
- lore guard


Current MVP focus:
- one playable era
- Kvothe only
- University + Imre slice
- grounded survival, study, work, and social consequence


===== FILE: hard_bans.md =====
No Anthropic. No OpenAI. No new AI provider.
Gemini remains the only optional LLM provider in this codebase.


No web server. No frontend unless a later prompt explicitly introduces it.
No random feature sprawl.
No major refactors.
Do not invent lore.
Do not resolve mysteries.
Do not add systems outside the prompt’s stated scope.
If a referenced file does not yet exist, skip it rather than inventing around it.


===== FILE: architecture_rule.md =====
engine/* owns:
- command parsing
- movement
- access checks
- time advancement
- state mutation
- persistence
- gameplay truth


content/* owns:
- authored registries and text pools only


narration/* owns:
- prose rendering only
- prompt construction only
- fallback rendering only
- canon and mystery safety only


Narration must never mutate PlayerState.
Narration must never write to the DB.
The engine remains the source of truth.
repl.ts saves state exactly once per turn.


===== FILE: architecture_reminders.md =====
These rules from Prompt 1 still apply:


- narration/* never reads from or writes to the DB
- narration/* never mutates PlayerState
- engine/* owns all state changes
- repl.ts saves state exactly once per turn
- No `any` types
- No external API calls outside the optional Gemini provider
- No unused imports or files
- Add tests alongside implementation


After Codex finishes, the commands it should run are basically:
python3 generate_prompts.py --start 21 --end 23 --dry-run
python3 generate_prompts.py --start 21 --end 25


One honest note: this will build you the generator and starter manifest, yes. But the quality of the final prompts still depends on how strong the manifest entries are. The generator gives you consistency, not wisdom. So the safest move is still to generate and review in blocks of 5, not all 21–40 in one hit.
If you want, the next most useful thing is a validator script that checks each manifest entry for thin scope, missing tests, duplicate systems, or likely roadmap drift before generation.






Good move. A validator will save you a lot of drift.
Here’s a paste-ready validate_manifest.py that works with the generator setup you already have. It checks:
* required fields
* field types
* duplicate prompt numbers
* duplicate titles
* empty sections
* weak/thin sections
* suspicious drift words like “refactor”, “rewrite”, “web app”, “frontend”
* duplicate new_files across prompts
* prompt range / gaps if you want 21–40 specifically
validate_manifest.py
#!/usr/bin/env python3
"""
Validate KKC prompt manifest quality before generation.


No third-party dependencies.
Checks:
- required fields
- field types
- duplicate prompt numbers/titles
- empty/thin sections
- suspicious scope drift phrases
- duplicate new_files across prompts
- optional expected prompt range (e.g. 21-40)


Usage:
  python3 validate_manifest.py
  python3 validate_manifest.py --manifest prompt_manifest_21_40.json
  python3 validate_manifest.py --start 21 --end 40
"""


from __future__ import annotations


import argparse
import json
import sys
from collections import defaultdict
from pathlib import Path
from typing import Any, Dict, List, Tuple




DEFAULT_MANIFEST = Path("prompt_manifest_21_40.json")


REQUIRED_FIELDS = {
    "number": int,
    "title": str,
    "phase_role": str,
    "goal": str,
    "goal_detail": str,
    "scope": list,
    "out_of_scope": list,
    "new_files": list,
    "modified_files": list,
    "implementation_sections": str,
    "tests": str,
    "success_criteria": list,
}


SUSPICIOUS_PHRASES = [
    "major refactor",
    "rewrite the architecture",
    "replace the architecture",
    "new web app",
    "frontend rewrite",
    "replace the engine",
    "switch frameworks",
    "new ai provider",
    "anthropic",
    "openai",
    "web server",
    "gui",
    "full natural-language parser",
    "randomly generate",
    "large-scale social simulation",
]


THIN_STRING_FIELDS = {
    "goal": 20,
    "goal_detail": 30,
    "implementation_sections": 40,
    "tests": 20,
}


THIN_LIST_FIELDS = {
    "scope": 2,
    "out_of_scope": 2,
    "new_files": 1,
    "modified_files": 1,
    "success_criteria": 2,
}




def load_manifest(path: Path) -> List[Dict[str, Any]]:
    if not path.exists():
        raise SystemExit(f"Manifest not found: {path}")
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError as exc:
        raise SystemExit(f"Manifest is not valid JSON: {exc}")
    if not isinstance(data, list):
        raise SystemExit("Manifest root must be a JSON array")
    return data




def norm_text(value: str) -> str:
    return " ".join(value.strip().split()).lower()




def is_nonempty_string(value: Any) -> bool:
    return isinstance(value, str) and value.strip() != ""




def validate_required_fields(entry: Dict[str, Any], idx: int) -> List[str]:
    errors: List[str] = []


    for field, expected_type in REQUIRED_FIELDS.items():
        if field not in entry:
            errors.append(f"Entry {idx}: missing required field '{field}'")
            continue


        value = entry[field]
        if not isinstance(value, expected_type):
            errors.append(
                f"Entry {idx} prompt {entry.get('number', '?')}: field '{field}' "
                f"must be {expected_type.__name__}"
            )
            continue


        if expected_type is str and not value.strip():
            errors.append(
                f"Entry {idx} prompt {entry.get('number', '?')}: field '{field}' is empty"
            )


        if expected_type is list and len(value) == 0:
            errors.append(
                f"Entry {idx} prompt {entry.get('number', '?')}: field '{field}' is empty"
            )


    return errors




def validate_content_quality(entry: Dict[str, Any]) -> Tuple[List[str], List[str]]:
    number = entry.get("number", "?")
    warnings: List[str] = []
    errors: List[str] = []


    for field, min_len in THIN_STRING_FIELDS.items():
        value = entry.get(field)
        if isinstance(value, str):
            stripped = value.strip()
            if len(stripped) < min_len:
                warnings.append(
                    f"Prompt {number}: field '{field}' looks thin ({len(stripped)} chars)"
                )


    for field, min_items in THIN_LIST_FIELDS.items():
        value = entry.get(field)
        if isinstance(value, list) and len(value) < min_items:
            warnings.append(
                f"Prompt {number}: field '{field}' looks thin ({len(value)} items)"
            )


    for field in ("scope", "out_of_scope", "new_files", "modified_files", "success_criteria"):
        value = entry.get(field)
        if isinstance(value, list):
            for i, item in enumerate(value):
                if not is_nonempty_string(item):
                    errors.append(
                        f"Prompt {number}: field '{field}[{i}]' must be a non-empty string"
                    )


    blob_parts = []
    for key in ("title", "phase_role", "goal", "goal_detail", "implementation_sections", "tests"):
        value = entry.get(key)
        if isinstance(value, str):
            blob_parts.append(value.lower())
    for key in ("scope", "out_of_scope", "new_files", "modified_files", "success_criteria"):
        value = entry.get(key)
        if isinstance(value, list):
            blob_parts.extend(str(v).lower() for v in value)


    blob = "\n".join(blob_parts)


    for phrase in SUSPICIOUS_PHRASES:
        if phrase in blob:
            warnings.append(
                f"Prompt {number}: suspicious phrase found -> '{phrase}'"
            )


    if isinstance(entry.get("tests"), str) and "test" not in entry["tests"].lower():
        warnings.append(f"Prompt {number}: tests section may be too vague")


    if isinstance(entry.get("success_criteria"), list):
        joined = " ".join(str(x).lower() for x in entry["success_criteria"])
        if "test" not in joined and "pass" not in joined:
            warnings.append(
                f"Prompt {number}: success_criteria do not explicitly mention tests"
            )


    return warnings, errors




def validate_duplicates(entries: List[Dict[str, Any]]) -> Tuple[List[str], List[str]]:
    errors: List[str] = []
    warnings: List[str] = []


    seen_numbers: Dict[int, int] = {}
    seen_titles: Dict[str, int] = {}
    new_file_owners: Dict[str, List[int]] = defaultdict(list)


    for entry in entries:
        number = entry.get("number")
        title = entry.get("title", "").strip()
        if isinstance(number, int):
            if number in seen_numbers:
                errors.append(f"Duplicate prompt number: {number}")
            seen_numbers[number] = 1


        title_key = norm_text(title)
        if title_key:
            if title_key in seen_titles:
                warnings.append(f"Duplicate or near-duplicate title: '{title}'")
            seen_titles[title_key] = 1


        for path in entry.get("new_files", []):
            if isinstance(path, str) and path.strip():
                new_file_owners[path.strip()].append(number)


    for path, owners in sorted(new_file_owners.items()):
        if len(owners) > 1:
            warnings.append(
                f"new_files path reused across prompts {owners}: {path}"
            )


    return warnings, errors




def validate_range(entries: List[Dict[str, Any]], start: int | None, end: int | None) -> List[str]:
    warnings: List[str] = []
    numbers = sorted(e["number"] for e in entries if isinstance(e.get("number"), int))


    if start is not None and end is not None:
        expected = list(range(start, end + 1))
        missing = [n for n in expected if n not in numbers]
        extra = [n for n in numbers if n < start or n > end]


        if missing:
            warnings.append(f"Missing prompt numbers in expected range: {missing}")
        if extra:
            warnings.append(f"Prompt numbers outside expected range: {extra}")


    for a, b in zip(numbers, numbers[1:]):
        if b != a + 1:
            warnings.append(f"Gap detected between prompt {a} and {b}")


    return warnings




def main() -> None:
    parser = argparse.ArgumentParser(description="Validate KKC prompt manifest")
    parser.add_argument("--manifest", type=Path, default=DEFAULT_MANIFEST, help="Path to manifest JSON")
    parser.add_argument("--start", type=int, help="Expected first prompt number")
    parser.add_argument("--end", type=int, help="Expected last prompt number")
    args = parser.parse_args()


    entries = load_manifest(args.manifest)


    all_errors: List[str] = []
    all_warnings: List[str] = []


    for idx, entry in enumerate(entries):
        all_errors.extend(validate_required_fields(entry, idx))
        warnings, errors = validate_content_quality(entry)
        all_warnings.extend(warnings)
        all_errors.extend(errors)


    dup_warnings, dup_errors = validate_duplicates(entries)
    all_warnings.extend(dup_warnings)
    all_errors.extend(dup_errors)


    all_warnings.extend(validate_range(entries, args.start, args.end))


    print(f"Validated {len(entries)} prompt entries from {args.manifest}")
    print()


    if all_errors:
        print("ERRORS:")
        for err in all_errors:
            print(f"  - {err}")
        print()
    else:
        print("ERRORS: none")
        print()


    if all_warnings:
        print("WARNINGS:")
        for warning in all_warnings:
            print(f"  - {warning}")
        print()
    else:
        print("WARNINGS: none")
        print()


    if all_errors:
        sys.exit(1)




if __name__ == "__main__":
    main()


Add this to your Codex prompt
Append this to the end of the big Codex prompt you already have:
Also create one more file:


- validate_manifest.py


Use the exact contents below:
[PASTE validate_manifest.py HERE]


After creating all files, run these commands too:


python3 validate_manifest.py --start 21 --end 40
python3 generate_prompts.py --start 21 --end 23 --dry-run
python3 generate_prompts.py --start 21 --end 25


If validation warnings appear, do not automatically rewrite the manifest unless there is a true correctness issue.
If validation errors appear, fix only the minimum needed.
Show me:
- validation results
- any fixes made
- generated filenames


Run it yourself later
python3 validate_manifest.py --start 21 --end 40
python3 generate_prompts.py --start 21 --end 25


What this gives you
It won’t magically judge whether a prompt is brilliant, but it will catch:
* broken manifest structure
* duplicated prompt numbers
* empty sections
* suspicious drift language
* reused new_files
* missing prompt numbers in the 21–40 range
That’s enough to stop a lot of mess early.
If you want, the next useful thing is a second validator that checks for roadmap drift more aggressively, like:
* Prompt 21 accidentally introducing delayed spread that belongs in 22
* Prompt 23 trying to add full research mechanics instead of just Archives access
* Prompt 31 sneaking in frontend work instead of CLI polish